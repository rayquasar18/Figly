---
phase: 11-bugfixes-ux-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/prisma/schema.prisma
  - packages/shared/src/dto/auth.dto.ts
  - backend/src/auth/dto/auth.dto.ts
  - backend/src/auth/auth.service.ts
  - backend/src/auth/auth.controller.ts
  - backend/src/email/templates/verification.ts
  - frontend/src/components/auth/signup-form.tsx
  - frontend/src/hooks/queries/auth-queries.ts
  - backend/src/auth/__tests__/auth.service.spec.ts
  - packages/shared/dist/dto/auth.dto.d.ts
  - packages/shared/dist/dto/auth.dto.js
autonomous: true
requirements:
  - BUGF-01
  - BUGF-02

must_haves:
  truths:
    - "Signup form only shows email and password fields -- no name, no username"
    - "Backend signup endpoint accepts {email, password} only and creates user with name: null, username: null"
    - "Verification email renders correctly when user.name is null (uses fallback greeting)"
    - "Existing users with name values are unaffected by the Prisma migration"
  artifacts:
    - path: "packages/shared/src/dto/auth.dto.ts"
      provides: "Simplified signupSchema with email+password only"
      contains: "signupSchema"
    - path: "backend/src/auth/dto/auth.dto.ts"
      provides: "Simplified SignupDto with email+password only"
      contains: "class SignupDto"
    - path: "backend/src/auth/auth.service.ts"
      provides: "signup() accepting {email, password} and creating user with null name/username"
      contains: "name: null"
    - path: "frontend/src/components/auth/signup-form.tsx"
      provides: "Signup form with email+password fields only"
      contains: "signupSchema"
  key_links:
    - from: "packages/shared/src/dto/auth.dto.ts"
      to: "frontend/src/components/auth/signup-form.tsx"
      via: "import signupSchema"
      pattern: "import.*signupSchema.*@figly/shared"
    - from: "backend/src/auth/auth.service.ts"
      to: "backend/prisma/schema.prisma"
      via: "prisma.user.create with name: null"
      pattern: "name: null"
    - from: "backend/src/auth/auth.controller.ts"
      to: "backend/src/email/templates/verification.ts"
      via: "sendVerificationEmail with null-safe name"
      pattern: "user\\.name \\|\\|"
---

<objective>
Simplify the signup flow to collect only email and password, deferring name and username to the complete-profile step. Update all four validation layers (Prisma schema, shared Zod, backend class-validator DTO, frontend form) in lockstep, and handle the null-name edge case in verification emails.

Purpose: Per D-01 through D-07, reduce signup friction by requiring only email+password at registration. Name and username are set later during the mandatory complete-profile step.
Output: Working signup flow accepting only email+password, with Prisma migration making User.name nullable.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-bugfixes-ux-flow/11-CONTEXT.md
@.planning/phases/11-bugfixes-ux-flow/11-RESEARCH.md
@.planning/phases/11-bugfixes-ux-flow/11-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From packages/shared/src/dto/auth.dto.ts (CURRENT -- will be modified):
```typescript
export const signupSchema = z.object({
  email: z.string().email({ message: 'Email khong hop le' }),
  password: passwordSchema,
  name: z.string().min(1, { message: 'Ten khong duoc de trong' }),
  username: usernameSchema,
});
export type SignupDto = z.infer<typeof signupSchema>;
```

From packages/shared/src/validators/username.ts (unchanged):
```typescript
export const usernameSchema = z.string()...;
export const USERNAME_RULES = { minLength: 3, maxLength: 30, pattern: /^[a-z0-9_.]+$/, cooldownDays: 14 };
```

From packages/shared/src/types/user.types.ts (unchanged):
```typescript
export interface PublicUser {
  id: string;
  email: string;
  name: string;
  username: string | null;
  emailVerified: boolean;
}
```

From backend/src/auth/auth.controller.ts line 36 (CURRENT -- needs null-safe name):
```typescript
await this.authService.sendVerificationEmail(user.id, user.email, user.name);
```

From backend/src/email/templates/verification.ts line 26 (CURRENT -- uses escapeHtml(name)):
```typescript
<h2>Xin chao ${escapeHtml(name)}!</h2>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Prisma schema, shared Zod, backend DTO, and backend service for email+password-only signup</name>
  <files>
    backend/prisma/schema.prisma,
    packages/shared/src/dto/auth.dto.ts,
    backend/src/auth/dto/auth.dto.ts,
    backend/src/auth/auth.service.ts,
    backend/src/auth/auth.controller.ts,
    backend/src/email/templates/verification.ts,
    backend/src/auth/__tests__/auth.service.spec.ts
  </files>
  <read_first>
    backend/prisma/schema.prisma,
    packages/shared/src/dto/auth.dto.ts,
    backend/src/auth/dto/auth.dto.ts,
    backend/src/auth/auth.service.ts,
    backend/src/auth/auth.controller.ts,
    backend/src/email/templates/verification.ts,
    backend/src/email/email.service.ts,
    backend/src/auth/__tests__/auth.service.spec.ts,
    backend/src/auth/__tests__/signup-username.spec.ts
  </read_first>
  <action>
    **1. Prisma schema (per D-05):** In `backend/prisma/schema.prisma`, change `name String` (line 25) to `name String?` (nullable). Then run `cd backend && npx prisma migrate dev --name make-name-nullable`. This makes the column nullable; existing users keep their current name values (per D-07).

    **2. Shared Zod schema (per D-03):** In `packages/shared/src/dto/auth.dto.ts`:
    - Remove the `import { usernameSchema } from '../validators/username';` line (no longer needed in this file).
    - Update `signupSchema` to:
      ```typescript
      export const signupSchema = z.object({
        email: z.string().email({ message: 'Email khong hop le' }),
        password: passwordSchema,
      });
      ```
    - The `SignupDto` type export stays as `z.infer<typeof signupSchema>` (will now infer to `{ email: string; password: string }`).
    - All other schemas (loginSchema, resetPasswordRequestSchema, etc.) remain unchanged.

    **3. Rebuild shared package:** Run `cd packages/shared && pnpm build` (or `npx tsc`) to regenerate `dist/` so frontend and backend pick up the new types.

    **4. Backend class-validator DTO (per D-04):** In `backend/src/auth/dto/auth.dto.ts`:
    - Remove the `name` field and its decorators (`@IsString()`, `@IsNotEmpty()`).
    - Remove the `username` field and its decorators (`@IsString()`, `@MinLength()`, `@MaxLength()`, `@Matches()`).
    - Keep only `email` and `password` fields. Final `SignupDto`:
      ```typescript
      export class SignupDto {
        @IsEmail({}, { message: 'Email khong hop le' })
        email!: string;

        @IsString()
        @MinLength(8, { message: 'Mat khau phai co it nhat 8 ky tu' })
        @Matches(/[a-zA-Z]/, { message: 'Mat khau phai chua chu cai' })
        @Matches(/[0-9]/, { message: 'Mat khau phai chua so' })
        password!: string;
      }
      ```
    - Remove unused imports: `IsNotEmpty`, `MaxLength` (if not used by other DTOs in the same file -- check first).

    **5. Backend auth.service.ts (per D-06):** In `backend/src/auth/auth.service.ts`:
    - Change `signup` method signature from `async signup(dto: { email: string; password: string; name: string; username: string })` to `async signup(dto: { email: string; password: string })`.
    - Remove the reserved username check block (lines 41-43): `if ((RESERVED_USERNAMES as readonly string[]).includes(dto.username.toLowerCase())) { ... }`. Remove the `RESERVED_USERNAMES` import if no longer used elsewhere in this file.
    - In `prisma.user.create`, change `data` from `{ email: dto.email, passwordHash, name: dto.name, username: dto.username, emailVerified: false }` to `{ email: dto.email, passwordHash, name: null, username: null, emailVerified: false }`.
    - In the `catch` block, remove the username unique constraint check (`if (error.meta?.target?.includes('username'))`), keeping only the email check.

    **6. Backend auth.controller.ts (null-safe email):** In `backend/src/auth/auth.controller.ts` line 36:
    - Change `await this.authService.sendVerificationEmail(user.id, user.email, user.name);` to `await this.authService.sendVerificationEmail(user.id, user.email, user.name || 'ban');`
    - Similarly, in `resendVerification` method (line 115): change `await this.authService.resendVerification(userId, email, user.name);` to `await this.authService.resendVerification(userId, email, user.name || 'ban');`

    **7. Email template (defensive):** In `backend/src/email/templates/verification.ts`, update `escapeHtml` call to handle null: the `name` parameter type stays `string` (the controller now passes fallback), but add a defensive fallback in the template function signature: change `export function renderVerificationEmail(name: string, verificationUrl: string)` -- no change needed since controller handles the fallback. BUT update the `escapeHtml` function to handle null input defensively: `function escapeHtml(str: string): string { if (!str) return ''; ... }`.

    **8. Update existing tests:** In `backend/src/auth/__tests__/auth.service.spec.ts`:
    - Find all `signupDto` test data objects that include `name` and `username` fields. Change them to `{ email: 'test@example.com', password: 'Test1234' }` (remove name and username).
    - Update any assertions that check `signup` was called with name/username.
    - Add a new test case: `it('should create user with null name and username', async () => { ... })` that verifies `prisma.user.create` is called with `data` containing `name: null, username: null`.

    In `backend/src/auth/__tests__/signup-username.spec.ts`:
    - The username validator tests (usernameSchema, USERNAME_RULES) remain valid -- they test the shared validator used by complete-profile, not signup.
    - If there are any tests that test username validation during signup flow, update or remove them. The shared username validator tests should remain unchanged.

    **9. Run tests:** `cd backend && npx jest --no-coverage` to verify all tests pass.
  </action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/Figly/backend && npx jest --no-coverage --passWithNoTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - backend/prisma/schema.prisma contains `name              String?` (with question mark, nullable)
    - A new migration file exists under backend/prisma/migrations/ with "make_name_nullable" in its name
    - packages/shared/src/dto/auth.dto.ts signupSchema contains ONLY email and password fields (no `name:`, no `username:`)
    - packages/shared/src/dto/auth.dto.ts does NOT import usernameSchema
    - backend/src/auth/dto/auth.dto.ts SignupDto class has only email and password properties (no name, no username)
    - backend/src/auth/auth.service.ts signup method signature is `async signup(dto: { email: string; password: string })`
    - backend/src/auth/auth.service.ts contains `name: null` and `username: null` in prisma.user.create call
    - backend/src/auth/auth.service.ts does NOT contain `RESERVED_USERNAMES` (or import is removed)
    - backend/src/auth/auth.controller.ts contains `user.name || 'ban'` in signup method
    - backend/src/auth/auth.controller.ts contains `user.name || 'ban'` in resendVerification method
    - backend/src/auth/__tests__/auth.service.spec.ts contains a test with description matching "null name" or "null username"
    - `cd backend && npx jest --no-coverage --passWithNoTests` exits with code 0
  </acceptance_criteria>
  <done>Backend accepts signup with only email+password, creates user with null name/username, sends verification email with "ban" fallback greeting. All backend tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Simplify frontend signup form and mutation to email+password only</name>
  <files>
    frontend/src/components/auth/signup-form.tsx,
    frontend/src/hooks/queries/auth-queries.ts
  </files>
  <read_first>
    frontend/src/components/auth/signup-form.tsx,
    frontend/src/hooks/queries/auth-queries.ts,
    packages/shared/src/dto/auth.dto.ts
  </read_first>
  <action>
    **1. Update signup form (per D-01):** In `frontend/src/components/auth/signup-form.tsx`:
    - Remove these imports that are no longer needed: `useEffect`, `useState` (keep the `useState` for `serverError` only -- remove the `debouncedUsername` state), `useCheckUsername` from profile-queries, `USERNAME_RULES` from @figly/shared, `Check`, `X`, `Loader2` from lucide-react (keep `Loader2` only if used elsewhere -- it's not, remove all three), `FormDescription` from ui/form.
    - Actually, keep `useState` import (still used for `serverError`). Remove `useEffect` import.
    - Remove all username-related state: `const [debouncedUsername, setDebouncedUsername] = useState('');`
    - Remove the debounce `useEffect` block entirely (lines 43-48).
    - Remove `watchedUsername`, `shouldCheck`, `usernameCheck`, `isCheckingUsername` variables.
    - Update `form.useForm<SignupDto>` defaultValues to only: `{ email: '', password: '' }` (remove `name: ''` and `username: ''`).
    - Remove the entire `name` FormField block (lines 82-99) -- the one with label "Ten".
    - Remove the entire `username` FormField block (lines 101-138) -- the one with label "Ten nguoi dung" and the @ prefix input.
    - Keep email and password FormField blocks unchanged.
    - Keep the submit button and error handling unchanged.
    - Reorder fields so email comes first, then password (they should already be in this order after removing name/username).
    - Remove unused imports after cleanup: `Check`, `X`, `Loader2`, `FormDescription`, `useCheckUsername`, `USERNAME_RULES`.
    - Keep `useEffect` import ONLY if still used -- after removing the debounce effect, check if any `useEffect` remains. If not, remove the import.

    Final imports should be:
    ```typescript
    'use client';
    import { useState } from 'react';
    import { useForm } from 'react-hook-form';
    import { zodResolver } from '@hookform/resolvers/zod';
    import { signupSchema, type SignupDto } from '@figly/shared';
    import { useSignupMutation } from '@/hooks/queries/auth-queries';
    import { Button } from '@/components/ui/button';
    import { Input } from '@/components/ui/input';
    import {
      Form, FormControl, FormField, FormItem, FormLabel, FormMessage,
    } from '@/components/ui/form';
    import { useRouter } from 'next/navigation';
    import { AxiosError } from 'axios';
    ```

    **2. Update useSignupMutation (per D-01/D-02):** In `frontend/src/hooks/queries/auth-queries.ts`:
    - Change the `mutationFn` parameter type from `(data: { email: string; password: string; name: string; username: string })` to `(data: { email: string; password: string })`.
    - The rest of the mutation remains unchanged.

    **3. Verify TypeScript compiles:** Run `cd frontend && npx tsc --noEmit` to verify no type errors.
  </action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/Figly/frontend && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/auth/signup-form.tsx does NOT contain `name="name"` (the form field name)
    - frontend/src/components/auth/signup-form.tsx does NOT contain `name="username"` (the form field name)
    - frontend/src/components/auth/signup-form.tsx does NOT contain `useCheckUsername`
    - frontend/src/components/auth/signup-form.tsx does NOT contain `USERNAME_RULES`
    - frontend/src/components/auth/signup-form.tsx does NOT contain `debouncedUsername`
    - frontend/src/components/auth/signup-form.tsx contains `defaultValues:` with only `email: ''` and `password: ''`
    - frontend/src/hooks/queries/auth-queries.ts useSignupMutation mutationFn accepts `{ email: string; password: string }` (no name, no username in type)
    - `cd frontend && npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>Signup form shows only email and password fields. No name or username inputs. TypeScript compiles cleanly with the simplified schema.</done>
</task>

</tasks>

<verification>
1. Backend tests pass: `cd backend && npx jest --no-coverage --passWithNoTests`
2. Frontend TypeScript compiles: `cd frontend && npx tsc --noEmit`
3. Shared package builds: `cd packages/shared && npx tsc`
4. Prisma migration applied: `cd backend && npx prisma migrate status` shows no pending migrations
</verification>

<success_criteria>
- Signup form renders only email and password fields (per D-01)
- Backend accepts signup with only {email, password} (per D-02)
- Shared signupSchema validates only email+password (per D-03)
- Backend SignupDto has only email+password (per D-04)
- Prisma User.name is nullable (per D-05)
- New users created with name: null, username: null (per D-06)
- Existing users unaffected (per D-07)
- Verification email uses "ban" fallback when name is null
- All backend tests pass
- Frontend TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-bugfixes-ux-flow/11-01-SUMMARY.md`
</output>
