---
phase: 16-shared-package-cleanup
plan: 02
subsystem: infra
tags: [gitignore, monorepo, shared-package, dist, build-artifacts]

# Dependency graph
requires:
  - phase: 01-foundation-auth
    provides: shared package initial setup with dist/ tracked in git
provides:
  - dist/ untracked from git (52 files removed from index)
  - packages/shared/.gitignore with dist/ entry
  - root .gitignore cleaned of !packages/shared/dist/ exception
  - .prettierignore cleaned of redundant packages/shared/dist entry
affects: [17-shared-package-removal, devops, ci-pipeline]

# Tech tracking
tech-stack:
  added: []
  patterns: [dist-not-tracked, build-artifacts-generated-on-fly]

key-files:
  created:
    - packages/shared/.gitignore
  modified:
    - .gitignore
    - .prettierignore

key-decisions:
  - "D-08 deferred: main field kept as ./dist/index.js because NestJS tsc compiler produces external require('@figly/shared') calls that Node.js resolves at runtime via main field -- Node cannot load .ts files natively"
  - "dist/ removal from git is independent of main field -- build pipeline creates dist/ on-the-fly via pnpm --filter @figly/shared build"

patterns-established:
  - "Build artifacts (dist/) are gitignored and generated by build pipeline, not tracked in version control"

requirements-completed: [SHRD-02]

# Metrics
duration: 3min
completed: 2026-03-26
---

# Phase 16 Plan 02: dist/ Removal Summary

**Removed 52 build artifacts from git tracking and configured gitignore to prevent re-adding; D-08 (main field change) deferred due to Docker runtime compatibility**

## Performance

- **Duration:** 3 min
- **Started:** 2026-03-25T23:45:51Z
- **Completed:** 2026-03-25T23:49:19Z
- **Tasks:** 2
- **Files modified:** 3 (+ 52 files removed from git index)

## Accomplishments
- Removed 52 packages/shared/dist/ files from git tracking without deleting from disk
- Created packages/shared/.gitignore with dist/ entry for local protection
- Removed !packages/shared/dist/ exception from root .gitignore -- dist/ rule now applies universally
- Cleaned .prettierignore of redundant packages/shared/dist entry
- Verified shared package rebuilds fresh dist/ from source successfully

## Task Commits

Each task was committed atomically:

1. **Task 1: Untrack dist/ from git and update gitignore files** - `5734238` (chore)
2. **Task 2: Update package.json entry points and verify builds** - no commit (D-08 deferred; package.json reverted to original after Docker incompatibility confirmed)

## Files Created/Modified
- `packages/shared/.gitignore` - New file with dist/ entry to prevent re-tracking
- `.gitignore` - Removed !packages/shared/dist/ exception (line 7)
- `.prettierignore` - Removed redundant packages/shared/dist entry (line 7)

## Decisions Made
- **D-08 deferred:** Changing main to ./src/index.ts breaks Docker production because NestJS uses tsc (not webpack), so compiled backend output keeps external `require('@figly/shared')` calls that Node.js resolves at runtime via the main field. Node.js cannot load .ts files natively. Recommend addressing in Phase 17 when shared package is fully removed.
- dist/ removal from git (Task 1) is completely independent of the main field value -- the build pipeline creates dist/ on-the-fly regardless.

## Deviations from Plan

None -- plan executed exactly as written, including the expected D-08 fallback path (Docker incompatibility confirmed via code inspection: backend/dist/ contains external `require("@figly/shared")` calls).

## Issues Encountered
- Docker daemon not running on this machine -- Docker build verification could not be performed. However, the failure mode was confirmed via direct code inspection: `grep -r "@figly/shared" backend/dist/` shows external require calls that would fail with main: ./src/index.ts at Node.js runtime.
- Backend build has pre-existing errors (class-validator missing imports, REEL_LIMITS not exported) unrelated to this plan's changes. These are out-of-scope pre-existing issues.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- dist/ is now properly gitignored and generated by build pipeline
- Phase 17 (shared package removal) can address the main field change when the shared package is eliminated entirely
- Pre-existing backend build errors (class-validator, REEL_LIMITS) should be tracked as tech debt

## Self-Check: PASSED

- packages/shared/.gitignore: FOUND
- 16-02-SUMMARY.md: FOUND
- Commit 5734238: FOUND
- dist files tracked: 0
- gitignore exception removed: confirmed
- prettierignore cleaned: confirmed

---
*Phase: 16-shared-package-cleanup*
*Completed: 2026-03-26*
