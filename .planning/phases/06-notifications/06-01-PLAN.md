---
phase: 06-notifications
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/prisma/schema.prisma
  - packages/shared/src/types/notification.types.ts
  - packages/shared/src/constants/notification.constants.ts
  - packages/shared/src/index.ts
  - backend/src/notifications/notifications.module.ts
  - backend/src/notifications/notifications.controller.ts
  - backend/src/notifications/notifications.service.ts
  - backend/src/notifications/notifications.processor.ts
  - backend/src/notifications/notifications.gateway.ts
  - backend/src/notifications/dto/notification-response.dto.ts
  - backend/src/notifications/push/push.service.ts
  - backend/src/notifications/push/push-subscription.dto.ts
  - backend/src/notifications/__tests__/notifications.service.spec.ts
  - backend/src/notifications/__tests__/notifications.processor.spec.ts
  - backend/src/notifications/__tests__/push.service.spec.ts
  - backend/src/app.module.ts
  - backend/src/config/configuration.ts
  - backend/src/posts/posts.service.ts
  - backend/src/posts/posts.module.ts
  - backend/src/comments/comments.service.ts
  - backend/src/comments/comments.module.ts
  - backend/src/social/social.service.ts
  - backend/src/social/social.module.ts
autonomous: true
requirements:
  - NOTF-01
  - NOTF-02
  - NOTF-03

must_haves:
  truths:
    - "Liking a post creates a notification for the post owner (not self)"
    - "Commenting on a post creates a notification for the post owner"
    - "Following a user creates a notification for the followed user"
    - "@mentioning a user in a comment creates a notification for the mentioned user"
    - "Notifications within 5-minute window on same target are grouped with multiple actors"
    - "SSE stream delivers notifications in real-time to connected recipients"
    - "Notification list returns paginated, chronological history with read/unread state"
    - "Mark-as-read and mark-all-as-read update notification state"
    - "Push subscription is stored and push notifications are sent to subscribed users"
  artifacts:
    - path: "backend/prisma/schema.prisma"
      provides: "Notification, NotificationActor, PushSubscription models"
      contains: "model Notification"
    - path: "backend/src/notifications/notifications.service.ts"
      provides: "Notification CRUD, grouping, mark-read, unread count"
      exports: ["NotificationsService"]
    - path: "backend/src/notifications/notifications.processor.ts"
      provides: "BullMQ worker for async notification creation"
      exports: ["NotificationsProcessor"]
    - path: "backend/src/notifications/notifications.gateway.ts"
      provides: "SSE per-user Subject map for real-time delivery"
      exports: ["NotificationsGateway"]
    - path: "backend/src/notifications/push/push.service.ts"
      provides: "web-push VAPID wrapper for push notification sending"
      exports: ["PushService"]
    - path: "packages/shared/src/types/notification.types.ts"
      provides: "NotificationResponse, NotificationActor shared types"
      exports: ["NotificationResponse", "NotificationActor", "NotificationType"]
  key_links:
    - from: "backend/src/posts/posts.service.ts"
      to: "notification BullMQ queue"
      via: "InjectQueue('notification') + queue.add on like/mention"
      pattern: "notificationQueue\\.add"
    - from: "backend/src/social/social.service.ts"
      to: "notification BullMQ queue"
      via: "InjectQueue('notification') + queue.add on follow"
      pattern: "notificationQueue\\.add"
    - from: "backend/src/comments/comments.service.ts"
      to: "notification BullMQ queue"
      via: "InjectQueue('notification') + queue.add on comment/reply"
      pattern: "notificationQueue\\.add"
    - from: "backend/src/notifications/notifications.processor.ts"
      to: "backend/src/notifications/notifications.gateway.ts"
      via: "gateway.emit after notification persist"
      pattern: "this\\.gateway\\.emit"
    - from: "backend/src/notifications/notifications.processor.ts"
      to: "backend/src/notifications/push/push.service.ts"
      via: "pushService.sendToUser after notification persist"
      pattern: "this\\.pushService\\.sendToUser"
---

<objective>
Build the complete backend notification infrastructure: Prisma schema (Notification, NotificationActor, PushSubscription models), NotificationsModule with BullMQ async processing, SSE real-time gateway, notification grouping/aggregation within 5-minute windows, REST endpoints for notification list/mark-read/unread-count, push notification service with web-push VAPID, and notification triggers wired into PostsService (like), CommentsService (comment/reply/mention), and SocialService (follow).

Purpose: Enables real-time in-app and push notification delivery for all social actions. The backend must be fully functional before the frontend can connect.
Output: Working NotificationsModule with SSE stream, REST API, BullMQ processor, push service, and all existing action services wired to emit notification jobs.
</objective>

<execution_context>
@/Users/mqcbook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mqcbook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-notifications/06-CONTEXT.md
@.planning/phases/06-notifications/06-RESEARCH.md
@.planning/phases/06-notifications/06-VALIDATION.md

@backend/src/app.module.ts
@backend/prisma/schema.prisma
@backend/src/config/configuration.ts
@backend/src/media/media.module.ts
@backend/src/media/media.processor.ts
@backend/src/posts/posts.service.ts
@backend/src/posts/posts.module.ts
@backend/src/comments/comments.service.ts
@backend/src/comments/comments.module.ts
@backend/src/social/social.service.ts
@backend/src/social/social.module.ts
@packages/shared/src/index.ts
@packages/shared/src/types/post.types.ts
@packages/shared/src/types/user.types.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From backend/src/media/media.module.ts (BullMQ queue registration pattern):
```typescript
BullModule.registerQueue({ name: 'media-processing' })
```

From backend/src/media/media.processor.ts (WorkerHost pattern):
```typescript
@Processor('media-processing')
export class MediaProcessor extends WorkerHost {
  async process(job: Job<MediaJobData>): Promise<void> { ... }
}
```

From backend/src/posts/posts.service.ts (notification trigger points):
```typescript
async toggleLike(userId: string, postId: string, like: boolean) { ... }
// After successful like creation -> enqueue notification job
```

From backend/src/social/social.service.ts:
```typescript
async follow(userId: string, targetUserId: string) { ... }
// After successful follow -> enqueue notification job
```

From backend/src/comments/comments.service.ts:
```typescript
async createComment(userId: string, postId: string, dto: CreateCommentDto) { ... }
// After successful comment/reply -> enqueue notification job
// Also detect @mentions in comment body -> enqueue mention notification jobs
```

From backend/src/config/configuration.ts:
```typescript
export default () => ({
  port: ..., jwt: ..., database: ..., redis: ..., minio: ..., resend: ..., google: ..., apple: ...
})
// Add: vapid: { publicKey, privateKey, subject }
```

From User model (schema.prisma line 16):
```prisma
model User {
  id String @id @default(cuid())
  // ... existing fields and relations ...
  // Add: notifications, notificationActors, pushSubscriptions relations
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema, shared types, and NotificationsModule with service + tests</name>
  <files>
    backend/prisma/schema.prisma,
    packages/shared/src/types/notification.types.ts,
    packages/shared/src/constants/notification.constants.ts,
    packages/shared/src/index.ts,
    backend/src/notifications/notifications.module.ts,
    backend/src/notifications/notifications.controller.ts,
    backend/src/notifications/notifications.service.ts,
    backend/src/notifications/notifications.gateway.ts,
    backend/src/notifications/dto/notification-response.dto.ts,
    backend/src/notifications/__tests__/notifications.service.spec.ts,
    backend/src/config/configuration.ts,
    backend/src/app.module.ts
  </files>
  <behavior>
    - Test: getNotifications returns paginated list with cursor, sorted by updatedAt desc
    - Test: getNotifications filters by recipientId only (no cross-user leakage)
    - Test: getUnreadCount returns count of isRead=false for a user
    - Test: markAsRead sets isRead=true for a specific notification owned by user
    - Test: markAllAsRead sets isRead=true for all notifications of a user
    - Test: findGroupableNotification returns existing notification within 5-min window for same groupKey
    - Test: findGroupableNotification returns null when no match or window expired
    - Test: createNotification persists notification with actor and emits SSE
    - Test: addActorToGroup adds actor to existing notification, updates timestamp, emits SSE
    - Test: self-notification skipped (actorId === recipientId returns early)
  </behavior>
  <action>
    1. **Install web-push:** Run `cd backend && pnpm add web-push && pnpm add -D @types/web-push`

    2. **Prisma schema extension:** Add to schema.prisma:
       - `NotificationType` enum: LIKE, COMMENT, REPLY, FOLLOW, MENTION
       - `Notification` model with id (cuid), recipientId, type, groupKey (string for aggregation key like "like:postId"), targetId (nullable), targetType (nullable "post"|"comment"), isRead (default false), createdAt, updatedAt. Indexes: [recipientId, isRead, createdAt], [groupKey, updatedAt]. Map to "notifications"
       - `NotificationActor` model with id (cuid), notificationId, actorId, createdAt. Unique constraint on [notificationId, actorId]. Index on [notificationId]. Map to "notification_actors"
       - `PushSubscription` model with id (cuid), userId, endpoint (@unique), p256dh, auth, createdAt. Index on [userId]. Map to "push_subscriptions"
       - Add relations to User model: notifications (Notification[] @relation("notificationRecipient")), notificationActors (NotificationActor[] @relation("notificationActor")), pushSubscriptions (PushSubscription[])
       - Run `cd backend && npx prisma db push`

    3. **Shared types** in `packages/shared/src/types/notification.types.ts`:
       - `NotificationType` = 'like' | 'comment' | 'reply' | 'follow' | 'mention'
       - `NotificationActorResponse` = { id, username, displayName, avatarUrl }
       - `NotificationResponse` = { id, type, actors (NotificationActorResponse[]), actorCount, targetId, targetType, targetThumbnail, message (Vietnamese), isRead, createdAt, updatedAt }
       - `UnreadCountResponse` = { count: number }

    4. **Shared constants** in `packages/shared/src/constants/notification.constants.ts`:
       - NOTIFICATION_LIMITS = { pageSize: 20, groupWindowMs: 300000 (5 min), maxDisplayActors: 3 }
       - Export from index.ts

    5. **NotificationsGateway** (injectable service, NOT a WebSocket gateway):
       - `Map<string, Subject<MessageEvent>>` for connected users
       - `subscribe(userId)`: returns Observable, creates Subject if missing, cleans up via finalize
       - `emit(userId, event)`: pushes to user's Subject if connected
       - `onModuleDestroy()`: completes all Subjects and clears map

    6. **NotificationsService:**
       - `getNotifications(userId, cursor?, limit=20)`: cursor pagination (take+1 pattern), ordered by updatedAt desc, includes actors (take 3, ordered by createdAt desc) with actor User select (id, username, name, avatarId), includes _count on actors. Resolve actor avatar presigned URLs and target post thumbnail presigned URL via StorageService
       - `getUnreadCount(userId)`: count where recipientId=userId AND isRead=false
       - `markAsRead(userId, notificationId)`: update where id=notificationId AND recipientId=userId, set isRead=true
       - `markAllAsRead(userId)`: updateMany where recipientId=userId AND isRead=false, set isRead=true
       - `findGroupableNotification(type, targetId, recipientId)`: find notification where groupKey=`${type}:${targetId}`, recipientId matches, and updatedAt > now - 5 minutes
       - `createNotification(data: NotificationJobData)`: create Notification + NotificationActor in transaction, compose groupKey, call gateway.emit
       - `addActorToGroup(notificationId, actorId)`: create NotificationActor (catch P2002 for idempotency), update notification updatedAt and isRead=false (re-surface grouped notifications), call gateway.emit
       - `composeMessage(type, actors, actorCount)`: compose Vietnamese notification text. Examples:
         - 1 actor like: "userA da thich bai viet cua ban"
         - 2 actors like: "userA va userB da thich bai viet cua ban"
         - 3+ actors like: "userA, userB va N nguoi khac da thich bai viet cua ban"
         - comment: "userA da binh luan ve bai viet cua ban"
         - reply: "userA da tra loi binh luan cua ban"
         - follow: "userA da theo doi ban"
         - mention: "userA da nhac den ban trong mot binh luan"
       - Import MediaModule's StorageService for presigned URL resolution (same pattern as ProfilesModule)

    7. **NotificationsController:**
       - `@Sse('stream')` with JwtAuthGuard: returns gateway.subscribe(userId). Pass req to extract user
       - `GET /notifications` with JwtAuthGuard: returns paginated notification list (query params: cursor, limit)
       - `GET /notifications/unread-count` with JwtAuthGuard: returns { count }
       - `PATCH /notifications/:id/read` with JwtAuthGuard: marks single notification read
       - `PATCH /notifications/read-all` with JwtAuthGuard: marks all read
       - `POST /notifications/push-subscription` with JwtAuthGuard: saves PushSubscription (endpoint, p256dh, auth)
       - `DELETE /notifications/push-subscription` with JwtAuthGuard: removes subscription by endpoint

    8. **NotificationsModule:**
       - Import BullModule.registerQueue({ name: 'notification' })
       - Import MediaModule for StorageService
       - Providers: NotificationsService, NotificationsProcessor, NotificationsGateway, PushService
       - Controllers: NotificationsController
       - Exports: NotificationsService (for future use), and export the BullModule queue registration so other modules can inject the queue

    9. **Configuration update** in `backend/src/config/configuration.ts`:
       - Add `vapid` block: { publicKey: process.env.VAPID_PUBLIC_KEY || '', privateKey: process.env.VAPID_PRIVATE_KEY || '', subject: process.env.VAPID_SUBJECT || 'mailto:admin@figly.app' }

    10. **AppModule update:** Import NotificationsModule

    11. **Unit tests** in `notifications.service.spec.ts`:
       - Mock PrismaService, NotificationsGateway, StorageService
       - Test all behaviors listed in `<behavior>` block
       - Use jest.spyOn for gateway.emit verification
  </action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/Figly/backend && npx jest src/notifications/__tests__/notifications.service.spec.ts --no-coverage -x</automated>
  </verify>
  <done>
    Prisma schema has Notification, NotificationActor, PushSubscription models with proper indexes. NotificationsService handles CRUD, grouping, mark-read with Vietnamese messages. NotificationsGateway manages SSE per-user connections. Controller exposes SSE stream + REST endpoints. All service tests pass. Configuration includes VAPID keys. Module registered in AppModule.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BullMQ processor, push service, notification triggers in existing services</name>
  <files>
    backend/src/notifications/notifications.processor.ts,
    backend/src/notifications/push/push.service.ts,
    backend/src/notifications/push/push-subscription.dto.ts,
    backend/src/notifications/__tests__/notifications.processor.spec.ts,
    backend/src/notifications/__tests__/push.service.spec.ts,
    backend/src/posts/posts.service.ts,
    backend/src/posts/posts.module.ts,
    backend/src/comments/comments.service.ts,
    backend/src/comments/comments.module.ts,
    backend/src/social/social.service.ts,
    backend/src/social/social.module.ts
  </files>
  <behavior>
    - Test (processor): process job with type "like" creates notification via service.createNotification
    - Test (processor): process job skips when actorId === recipientId (self-notification guard)
    - Test (processor): process job with groupable existing notification calls service.addActorToGroup
    - Test (processor): process job calls pushService.sendToUser after notification creation
    - Test (push): sendToUser sends web-push notification to all user subscriptions
    - Test (push): sendToUser handles 410 Gone by deleting expired subscription
    - Test (push): sendToUser silently handles errors without crashing processor
    - Test (push): saveSubscription upserts by endpoint
    - Test (push): removeSubscription deletes by endpoint + userId
  </behavior>
  <action>
    1. **PushSubscription DTO** in `push-subscription.dto.ts`:
       - class SavePushSubscriptionDto with endpoint (string, IsUrl), p256dh (string), auth (string)
       - class RemovePushSubscriptionDto with endpoint (string, IsUrl)

    2. **PushService** in `push/push.service.ts`:
       - Constructor: inject PrismaService, ConfigService. On module init, call `webpush.setVapidDetails(subject, publicKey, privateKey)` using config values. If keys are empty, log warning and disable push (dev mode graceful degradation)
       - `saveSubscription(userId, dto)`: upsert PushSubscription by endpoint, set userId/p256dh/auth
       - `removeSubscription(userId, endpoint)`: delete where endpoint AND userId
       - `sendToUser(userId, payload: { title, body, url })`: find all PushSubscription for userId, for each call `webpush.sendNotification(subscription, JSON.stringify(payload))`. Catch errors: if statusCode 410 or 404, delete that subscription. Other errors logged but swallowed (push failures must not crash the processor)
       - `getVapidPublicKey()`: return public key from config (for frontend to use if needed)

    3. **NotificationsProcessor** in `notifications.processor.ts`:
       - `@Processor('notification')` extending WorkerHost
       - Inject NotificationsService, PushService
       - `process(job: Job<NotificationJobData>)`:
         a. Destructure { type, actorId, recipientId, targetId, targetType } from job.data
         b. Guard: if actorId === recipientId, return (skip self-notifications)
         c. Call service.findGroupableNotification(type, targetId, recipientId)
         d. If groupable found: call service.addActorToGroup(existing.id, actorId)
         e. Else: call service.createNotification(job.data)
         f. Compose push message: get actor's name from DB, build title "Figly" and body (Vietnamese action text)
         g. Call pushService.sendToUser(recipientId, { title, body, url }) where url is the deep link (/post/:id, /:username, etc.)
       - Logger for job processing (same pattern as MediaProcessor)
       - `NotificationJobData` type: { type: NotificationType, actorId: string, recipientId: string, targetId?: string, targetType?: string }

    4. **Wire notification triggers into existing services.** Each service module needs to import BullModule.registerQueue({ name: 'notification' }) and inject the queue. Use `@InjectQueue('notification') private notificationQueue: Queue` in service constructors.

       **PostsService.toggleLike** -- after successful like creation (the `create` branch, not the `delete` branch):
       ```
       await this.notificationQueue.add('notification', {
         type: 'like',
         actorId: userId,
         recipientId: post.authorId,
         targetId: postId,
         targetType: 'post',
       });
       ```
       Need to fetch post.authorId before enqueuing. The toggleLike method already has the postId; add a findUnique to get authorId (or if already available in the method, use it).

       **CommentsService.createComment** -- after comment creation:
       - If comment has parentId (is a reply): enqueue type='reply', recipientId=parentComment.authorId, targetId=postId, targetType='post'
       - If no parentId (top-level comment): enqueue type='comment', recipientId=post.authorId, targetId=postId, targetType='post'
       - Detect @mentions in comment body using regex `/@(\w+)/g`, resolve usernames to userIds, enqueue type='mention' for each mentioned user (skip duplicates, skip self, skip the already-notified post owner/parent author)

       **SocialService.follow** -- after successful follow creation (catch P2002 means already following, don't re-notify):
       ```
       await this.notificationQueue.add('notification', {
         type: 'follow',
         actorId: userId,
         recipientId: targetUserId,
       });
       ```
       Only enqueue if the follow was newly created (not if P2002 was caught for idempotent re-follow).

    5. **Update module imports:**
       - PostsModule: add `BullModule.registerQueue({ name: 'notification' })` to imports
       - CommentsModule: add `BullModule.registerQueue({ name: 'notification' })` to imports
       - SocialModule: add `BullModule.registerQueue({ name: 'notification' })` to imports

    6. **Unit tests:**
       - `notifications.processor.spec.ts`: mock NotificationsService, PushService, PrismaService. Test all processor behaviors
       - `push.service.spec.ts`: mock PrismaService, ConfigService, web-push module. Test save/remove/send behaviors including 410 cleanup
  </action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/Figly/backend && npx jest --testPathPattern="notifications" --no-coverage -x</automated>
  </verify>
  <done>
    BullMQ processor creates/groups notifications and sends push. PushService wraps web-push with 410 cleanup. PostsService enqueues notification on like. CommentsService enqueues on comment/reply/mention. SocialService enqueues on follow. Self-notification guard active in processor. All notification tests pass. Full backend test suite passes.
  </done>
</task>

</tasks>

<verification>
1. Run all notification tests: `cd backend && npx jest --testPathPattern="notifications|push" --no-coverage -x`
2. Run full backend test suite: `cd backend && npx jest --no-coverage`
3. Verify Prisma schema pushes cleanly: `cd backend && npx prisma db push`
4. Verify SSE endpoint accessible: `curl -N -H "Cookie: ..." http://localhost:4000/api/notifications/stream` returns text/event-stream
5. Verify notification list endpoint: `curl http://localhost:4000/api/notifications` returns paginated JSON
</verification>

<success_criteria>
- Notification, NotificationActor, PushSubscription models exist in Prisma schema with proper indexes
- NotificationsModule registered in AppModule with BullMQ queue
- SSE stream endpoint at GET /notifications/stream returns Observable
- REST endpoints for list, unread-count, mark-read, mark-all-read, push-subscription work
- Liking a post enqueues a notification job (verified via test)
- Commenting enqueues comment/reply notification job (verified via test)
- Following enqueues follow notification job (verified via test)
- @mentions in comments enqueue mention notification jobs (verified via test)
- Self-notifications are skipped (actorId === recipientId)
- Grouping within 5-min window aggregates actors on same target
- Push notifications sent via web-push with 410 cleanup
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-notifications/06-01-SUMMARY.md`
</output>
