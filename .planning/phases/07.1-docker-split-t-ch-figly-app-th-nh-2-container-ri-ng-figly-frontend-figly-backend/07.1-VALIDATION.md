---
phase: 07.1
slug: docker-split
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-16
---

# Phase 07.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Docker Compose integration test (shell commands) |
| **Config file** | docker-compose.yml |
| **Quick run command** | `docker compose build` |
| **Full suite command** | `docker compose build && docker compose up -d && sleep 10 && docker compose ps` |
| **Estimated runtime** | ~120 seconds |

---

## Sampling Rate

- **After every task commit:** Run `docker compose build`
- **After every plan wave:** Run `docker compose build && docker compose up -d && sleep 10 && docker compose ps`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 120 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 07.1-01-01 | 01 | 1 | INFRA | smoke | `docker compose ps frontend` + `curl -f http://localhost:3000` | N/A (Docker) | ⬜ pending |
| 07.1-01-02 | 01 | 1 | INFRA | smoke | `docker compose ps backend` + `curl -f http://localhost:4000/api` | N/A (Docker) | ⬜ pending |
| 07.1-01-03 | 01 | 1 | INFRA | config | `docker compose config --services` | N/A (Docker) | ⬜ pending |
| 07.1-01-04 | 01 | 1 | INFRA | integration | `docker compose up -d && curl http://localhost:4000/api/health` | N/A (Docker) | ⬜ pending |
| 07.1-01-05 | 01 | 1 | INFRA | e2e | `docker compose build && docker compose up -d && docker compose ps` | N/A (Docker) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

*Existing infrastructure covers all phase requirements.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Frontend loads in browser | INFRA | Requires visual verification | Open http://localhost:3000 in browser, confirm app renders |
| WebSocket connectivity | INFRA | Requires browser runtime | Open app, check browser console for Socket.IO connection |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
