---
phase: 14-frontend-restructure
plan: 04
type: execute
wave: 2
depends_on: [14-01, 14-02]
files_modified:
  - frontend/src/app/(app)/page.tsx
  - frontend/src/app/(app)/reels/page.tsx
  - frontend/src/app/(app)/saved/page.tsx
  - frontend/src/app/(app)/complete-profile/page.tsx
  - frontend/src/app/(app)/checklists/page.tsx
  - frontend/src/app/(app)/checklists/new/page.tsx
  - frontend/src/app/(app)/checklists/[checklistId]/page.tsx
  - frontend/src/app/(auth)/login/page.tsx
  - frontend/src/app/(auth)/signup/page.tsx
  - frontend/src/app/(auth)/verify-email/page.tsx
  - frontend/src/app/(auth)/forgot-password/page.tsx
  - frontend/src/app/(auth)/reset-password/page.tsx
autonomous: true
requirements: [FRNT-03]

must_haves:
  truths:
    - "Authenticated pages render as Server Components at the page level"
    - "Auth pages (login, signup, etc.) render as Server Components at the page level"
    - "Server Components fetch initial data via fetchApi and pass to client components"
    - "All existing interactive functionality preserved in client components"
    - "No 'use client' directive on any page.tsx file in the entire app"
  artifacts:
    - path: "frontend/src/app/(app)/page.tsx"
      provides: "Server Component home feed page"
      min_lines: 10
    - path: "frontend/src/app/(app)/reels/page.tsx"
      provides: "Server Component reels page"
      min_lines: 10
    - path: "frontend/src/app/(app)/checklists/[checklistId]/page.tsx"
      provides: "Server Component checklist detail"
      min_lines: 10
    - path: "frontend/src/app/(auth)/login/page.tsx"
      provides: "Server Component login page"
      min_lines: 5
  key_links:
    - from: "frontend/src/app/(app)/page.tsx"
      to: "frontend/src/lib/server-fetch.ts"
      via: "fetchApi for authenticated server-side data fetch"
      pattern: "fetchApi"
    - from: "frontend/src/app/(app)/*.tsx"
      to: "frontend/src/features/*"
      via: "barrel imports for feature components"
      pattern: "@/features/"
---

<objective>
Convert all 12 remaining pages (7 authenticated + 5 auth) from 'use client' to async Server Components, extracting interactive parts to client components. Per D-13 and D-18, ALL pages become Server Components -- not just public ones.

Purpose: Completes FRNT-03 by converting the remaining pages. After this plan, zero page.tsx files in the entire app will have 'use client'. Authenticated pages get server-side initial data fetching for faster perceived load times.
Output: 12 pages converted to Server Components, client component extractions.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-frontend-restructure/14-CONTEXT.md
@.planning/phases/14-frontend-restructure/14-RESEARCH.md
@.planning/phases/14-frontend-restructure/14-01-SUMMARY.md
@.planning/phases/14-frontend-restructure/14-02-SUMMARY.md

@frontend/src/lib/server-fetch.ts
</context>

<interfaces>
<!-- Key types and contracts the executor needs. -->

From frontend/src/lib/server-fetch.ts (created in Plan 01):
```typescript
export async function fetchApi<T>(path: string, options?: RequestInit): Promise<T | null>;
```

Feature barrel imports (created in Plan 02):
```typescript
import { FeedList } from '@/features/feed';
import { ReelFeed, ReelSkeleton } from '@/features/reel';
import { CompleteProfileForm } from '@/features/profile';
import { ChecklistCard, ChecklistEntry, ChecklistForm } from '@/features/checklist';
import { LoginForm, SignupForm, SocialLoginButtons } from '@/features/auth';
import { ItemPicker } from '@/features/collection';
import { PostDetailModal } from '@/features/post';
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Convert authenticated (app) pages to Server Components</name>
  <files>
    frontend/src/app/(app)/page.tsx,
    frontend/src/app/(app)/reels/page.tsx,
    frontend/src/app/(app)/saved/page.tsx,
    frontend/src/app/(app)/complete-profile/page.tsx,
    frontend/src/app/(app)/checklists/page.tsx,
    frontend/src/app/(app)/checklists/new/page.tsx,
    frontend/src/app/(app)/checklists/[checklistId]/page.tsx
  </files>
  <read_first>
    - frontend/src/app/(app)/page.tsx
    - frontend/src/app/(app)/reels/page.tsx
    - frontend/src/app/(app)/saved/page.tsx
    - frontend/src/app/(app)/complete-profile/page.tsx
    - frontend/src/app/(app)/checklists/page.tsx
    - frontend/src/app/(app)/checklists/new/page.tsx
    - frontend/src/app/(app)/checklists/[checklistId]/page.tsx
    - frontend/src/lib/server-fetch.ts
  </read_first>
  <action>
Apply the same conversion pattern: remove 'use client', make async, fetch initial data with fetchApi, extract interactive code to *-client.tsx.

**Page 1: `(app)/page.tsx` (Home feed)**
Currently 11 lines. Remove 'use client'. The page is simple -- it renders FeedList.
- Make async Server Component
- Fetch initial feed: `const initialFeed = await fetchApi<PaginatedResponse<PostResponse>>('/feed?limit=20');`
- Extract to `feed-page-client.tsx` with 'use client' that wraps FeedList with initialData
- Server Component: `export default async function FeedPage() { ... return <FeedPageClient initialFeed={initialFeed} />; }`

**Page 2: `(app)/reels/page.tsx` (Reels)**
Currently 23 lines. Remove 'use client'.
- Fetch initial reels: `const initialReels = await fetchApi<PaginatedResponse<any>>('/feed/reels?limit=10');`
- Extract to `reels-page-client.tsx` with 'use client'
- Passes initialReels and renders ReelFeed

**Page 3: `(app)/saved/page.tsx` (Saved posts)**
Currently 128 lines. Remove 'use client'.
- Fetch initial saved posts: `const initialSaved = await fetchApi<PaginatedResponse<PostResponse>>('/posts/saved?limit=20');`
- Extract ALL interactive logic (infinite scroll, state, handlers) to `saved-page-client.tsx` with 'use client'
- Server Component: `return <SavedPageClient initialSaved={initialSaved} />;`

**Page 4: `(app)/complete-profile/page.tsx`**
Currently 24 lines. Remove 'use client'.
- No server data fetch needed (form page)
- Extract to `complete-profile-page-client.tsx` with 'use client' that renders CompleteProfileForm
- Server Component just returns the client component wrapper

**Page 5: `(app)/checklists/page.tsx` (Checklist list)**
Currently 62 lines. Remove 'use client'.
- Fetch initial checklists: `const initialChecklists = await fetchApi<any[]>('/checklists');`
- Extract to `checklists-page-client.tsx` with 'use client'

**Page 6: `(app)/checklists/new/page.tsx` (New checklist)**
Currently 29 lines. Remove 'use client'.
- No server data fetch needed (form page)
- Extract to `new-checklist-page-client.tsx` with 'use client'

**Page 7: `(app)/checklists/[checklistId]/page.tsx` (Checklist detail)**
Currently 299 lines -- largest page. Remove 'use client'.
- Fetch initial checklist: `const { checklistId } = await params; const initialChecklist = await fetchApi<any>(`/checklists/${checklistId}`);`
- Extract ALL interactive logic to `checklist-detail-page-client.tsx` with 'use client'
- This is the most complex extraction -- ensure all useState, useEffect, mutation hooks, event handlers are moved to the client component

**For ALL client component extractions:** Read the existing code carefully. The client component must:
1. Have `'use client'` directive
2. Accept initial data as props (e.g., `initialFeed`, `initialChecklist`)
3. Use the existing React Query hooks with `initialData` option
4. Preserve ALL existing functionality (handlers, effects, state, JSX)
5. Import from `@/features/*` barrels (not old paths)

**Key consideration for authenticated pages:** The fetchApi utility forwards cookies from the request, so authenticated endpoints will work. If the user has no valid access_token cookie, fetchApi returns null -- the client component handles this gracefully (shows loading state while React Query refetches on the client).
  </action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/Figly/frontend && PAGES="src/app/(app)/page.tsx src/app/(app)/reels/page.tsx src/app/(app)/saved/page.tsx src/app/(app)/complete-profile/page.tsx src/app/(app)/checklists/page.tsx src/app/(app)/checklists/new/page.tsx src/app/(app)/checklists/[checklistId]/page.tsx"; NO_CLIENT=0; for p in $PAGES; do grep -q "'use client'" "$p" || NO_CLIENT=$((NO_CLIENT+1)); done; echo "No 'use client': $NO_CLIENT/7"; if [ "$NO_CLIENT" -eq 7 ]; then echo "PASS"; else echo "FAIL"; fi</automated>
  </verify>
  <acceptance_criteria>
    - All 7 `(app)` page.tsx files do NOT contain `'use client'`
    - `frontend/src/app/(app)/page.tsx` is an async function that returns a client component
    - `frontend/src/app/(app)/checklists/[checklistId]/page.tsx` does NOT contain `'use client'` and contains `await params`
    - Client component files exist for each page (e.g., feed-page-client.tsx, reels-page-client.tsx, saved-page-client.tsx, checklist-detail-page-client.tsx)
    - Each client component file DOES contain `'use client'`
    - At least 4 of the 7 pages import `fetchApi` from `@/lib/server-fetch` (complete-profile and new-checklist may not need it)
    - All imports use `@/features/*` barrel paths (no old component/hooks/stores paths)
  </acceptance_criteria>
  <done>All 7 authenticated pages converted to Server Components. Interactive parts extracted to client components. Initial data fetched server-side with fetchApi and passed as initialData props.</done>
</task>

<task type="auto">
  <name>Task 2: Convert auth pages to Server Components</name>
  <files>
    frontend/src/app/(auth)/login/page.tsx,
    frontend/src/app/(auth)/signup/page.tsx,
    frontend/src/app/(auth)/verify-email/page.tsx,
    frontend/src/app/(auth)/forgot-password/page.tsx,
    frontend/src/app/(auth)/reset-password/page.tsx
  </files>
  <read_first>
    - frontend/src/app/(auth)/login/page.tsx
    - frontend/src/app/(auth)/signup/page.tsx
    - frontend/src/app/(auth)/verify-email/page.tsx
    - frontend/src/app/(auth)/forgot-password/page.tsx
    - frontend/src/app/(auth)/reset-password/page.tsx
    - frontend/src/app/(auth)/layout.tsx
  </read_first>
  <action>
Auth pages are simpler -- they're mostly form wrappers. Convert to Server Components with static metadata.

**Page 1: `(auth)/login/page.tsx`**
Currently 52 lines. Remove 'use client'. Add metadata:
```typescript
export const metadata: Metadata = {
  title: 'Dang nhap | Figly',
  description: 'Dang nhap vao Figly de chia se va quan ly bo suu tap cua ban',
};
```
Extract interactive logic (if any useState/useEffect) to `login-page-client.tsx`, or if the page is just wrapping LoginForm + SocialLoginButtons, extract the wrapper:
```typescript
// login-page-client.tsx
'use client';
import { LoginForm, SocialLoginButtons } from '@/features/auth';
// ... existing layout/JSX
```
Server Component: `return <LoginPageClient />;`

**Page 2: `(auth)/signup/page.tsx`**
Currently 34 lines. Remove 'use client'. Add metadata:
```typescript
export const metadata: Metadata = {
  title: 'Dang ky | Figly',
  description: 'Tao tai khoan Figly de bat dau chia se bo suu tap',
};
```
Extract to `signup-page-client.tsx`.

**Page 3: `(auth)/verify-email/page.tsx`**
Currently 107 lines. Remove 'use client'. Add metadata:
```typescript
export const metadata: Metadata = {
  title: 'Xac thuc email | Figly',
};
```
Extract all interactive logic (useEffect for auto-verification, mutation hooks) to `verify-email-page-client.tsx`.

**Page 4: `(auth)/forgot-password/page.tsx`**
Currently 116 lines. Remove 'use client'. Add metadata:
```typescript
export const metadata: Metadata = {
  title: 'Quen mat khau | Figly',
  description: 'Dat lai mat khau tai khoan Figly',
};
```
Extract form logic to `forgot-password-page-client.tsx`.

**Page 5: `(auth)/reset-password/page.tsx`**
Currently 173 lines. Remove 'use client'. Add metadata:
```typescript
export const metadata: Metadata = {
  title: 'Dat lai mat khau | Figly',
};
```
Extract form + token validation logic to `reset-password-page-client.tsx`.

**For auth pages:** These don't need fetchApi (no server data to prefetch). The Server Component simply renders the client wrapper. The main benefit is adding static SEO metadata and following the consistent pattern (per D-13: ALL pages are Server Components).

**Also update `(auth)/layout.tsx`:** Read the current auth layout. If it has 'use client' and useMe hook for redirect-if-logged-in logic, keep it as 'use client' (it's a layout, not a page). The proxy.ts already handles auth redirect for protected routes, but the auth layout needs the reverse check (redirect away from login if already authenticated). Per D-11, keep client-side auth checks as defense-in-depth.

**Final verification:** After all conversions, run:
```
grep -rn "'use client'" frontend/src/app/ --include="page.tsx"
```
This MUST return ZERO results -- no page.tsx file should have 'use client'.
  </action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/Figly/frontend && TOTAL=$(grep -rn "'use client'" src/app/ --include="page.tsx" | wc -l); echo "Pages with 'use client': $TOTAL"; if [ "$TOTAL" -eq 0 ]; then echo "PASS - all pages are Server Components"; else echo "FAIL - $TOTAL pages still have 'use client'"; fi</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "'use client'" frontend/src/app/ --include="page.tsx"` returns ZERO results (ALL 21 pages are Server Components)
    - `frontend/src/app/(auth)/login/page.tsx` contains `export const metadata` with title 'Dang nhap | Figly'
    - `frontend/src/app/(auth)/signup/page.tsx` contains `export const metadata` with title 'Dang ky | Figly'
    - `frontend/src/app/(auth)/verify-email/page.tsx` contains `export const metadata` with title 'Xac thuc email | Figly'
    - `frontend/src/app/(auth)/forgot-password/page.tsx` contains `export const metadata` with title 'Quen mat khau | Figly'
    - `frontend/src/app/(auth)/reset-password/page.tsx` contains `export const metadata` with title 'Dat lai mat khau | Figly'
    - Client component files exist for each auth page with `'use client'` directive
    - `frontend/src/app/(auth)/layout.tsx` still has client-side auth redirect logic (defense-in-depth per D-11)
  </acceptance_criteria>
  <done>All 5 auth pages converted to Server Components with static SEO metadata. Total: 21/21 page.tsx files are now Server Components. Zero 'use client' directives remain in any page.tsx file across the entire app.</done>
</task>

</tasks>

<verification>
1. `grep -rn "'use client'" frontend/src/app/ --include="page.tsx"` returns ZERO results
2. `grep -rn "metadata\|generateMetadata" frontend/src/app/ --include="page.tsx" | wc -l` returns 21 (every page has metadata)
3. `cd frontend && pnpm tsc --noEmit` passes (or only pre-existing errors)
4. `cd frontend && pnpm build` completes successfully
</verification>

<success_criteria>
- All 21 page.tsx files across (app)/, (auth)/, and (public)/ are Server Components (no 'use client')
- All authenticated pages fetch initial data server-side via fetchApi with cookie forwarding
- All auth pages have static metadata for SEO
- All interactive parts properly extracted to *-client.tsx files
- Client components use React Query initialData for hydration
- TypeScript compilation passes
- Build completes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/14-frontend-restructure/14-04-SUMMARY.md`
</output>
