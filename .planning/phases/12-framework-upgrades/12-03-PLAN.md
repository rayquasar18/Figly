---
phase: 12-framework-upgrades
plan: 03
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - frontend/src/app/(public)/post/[postId]/page.tsx
  - frontend/src/app/(public)/item/[itemId]/page.tsx
  - frontend/src/app/(public)/collection/[categorySlug]/page.tsx
  - frontend/src/app/(public)/collection/[categorySlug]/[seriesSlug]/page.tsx
autonomous: true
requirements: [FRMW-02]
must_haves:
  truths:
    - "All 4 pages use async params pattern with use() hook"
    - "Post detail page loads correctly with dynamic postId"
    - "Item detail page loads correctly with dynamic itemId"
    - "Collection category page loads correctly with dynamic categorySlug"
    - "Collection series page loads correctly with dynamic categorySlug and seriesSlug"
    - "Frontend builds without errors"
  artifacts:
    - path: "frontend/src/app/(public)/post/[postId]/page.tsx"
      provides: "Post detail page with async params"
      contains: "use(params)"
    - path: "frontend/src/app/(public)/item/[itemId]/page.tsx"
      provides: "Item detail page with async params"
      contains: "use(params)"
    - path: "frontend/src/app/(public)/collection/[categorySlug]/page.tsx"
      provides: "Collection category page with async params"
      contains: "use(params)"
    - path: "frontend/src/app/(public)/collection/[categorySlug]/[seriesSlug]/page.tsx"
      provides: "Collection series page with async params"
      contains: "use(params)"
  key_links:
    - from: "frontend/src/app/(public)/post/[postId]/page.tsx"
      to: "react"
      via: "use() hook"
      pattern: "import.*use.*from.*react"
    - from: "frontend/src/app/(public)/collection/[categorySlug]/page.tsx"
      to: "react"
      via: "use() hook"
      pattern: "import.*use.*from.*react"
---

<objective>
Migrate 4 remaining page components from the deprecated useParams() hook to Next.js 15+/16 async params pattern using React's use() hook.

Purpose: Next.js 16 requires the async params pattern. While useParams() still works in client components, it is the deprecated pattern. Migrating now ensures consistency with the 3 pages already using the new pattern (profile, followers, following pages).

Output: All dynamic route pages use the `use(params)` pattern with Promise-typed params prop.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-framework-upgrades/12-RESEARCH.md

<interfaces>
<!-- Already-migrated page showing the correct pattern -->

From frontend/src/app/(app)/[username]/page.tsx (ALREADY CORRECT):
```typescript
'use client';
import { use } from 'react';

export default function ProfilePage({
  params,
}: {
  params: Promise<{ username: string }>;
}) {
  const { username } = use(params);
  // ... rest of component
}
```

Pages to migrate (OLD pattern):
```typescript
// post/[postId]/page.tsx - currently:
import { useParams, useRouter } from 'next/navigation';
const params = useParams();
const postId = params.postId as string;

// item/[itemId]/page.tsx - currently:
import { useParams, useRouter } from 'next/navigation';
const params = useParams<{ itemId: string }>();

// collection/[categorySlug]/page.tsx - currently:
import { useParams } from 'next/navigation';
const params = useParams<{ categorySlug: string }>();

// collection/[categorySlug]/[seriesSlug]/page.tsx - currently:
import { useParams } from 'next/navigation';
const params = useParams<{ categorySlug: string; seriesSlug: string }>();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate 4 pages from useParams to async params with use() hook</name>
  <read_first>
    - frontend/src/app/(public)/post/[postId]/page.tsx
    - frontend/src/app/(public)/item/[itemId]/page.tsx
    - frontend/src/app/(public)/collection/[categorySlug]/page.tsx
    - frontend/src/app/(public)/collection/[categorySlug]/[seriesSlug]/page.tsx
    - frontend/src/app/(app)/[username]/page.tsx
  </read_first>
  <files>
    frontend/src/app/(public)/post/[postId]/page.tsx,
    frontend/src/app/(public)/item/[itemId]/page.tsx,
    frontend/src/app/(public)/collection/[categorySlug]/page.tsx,
    frontend/src/app/(public)/collection/[categorySlug]/[seriesSlug]/page.tsx
  </files>
  <action>
For each of the 4 pages, apply the following migration:

**Step 1:** Add `use` to the React import (or add a new React import if not present):
```typescript
import { use } from 'react';
```

**Step 2:** Remove `useParams` from the `next/navigation` import. If `useParams` was the only import from `next/navigation`, remove the entire import line. If other imports exist (like `useRouter`), keep those.

**Step 3:** Change the component signature from:
```typescript
export default function PageName() {
  const params = useParams();
  const paramValue = params.paramName as string;
```
To:
```typescript
export default function PageName({
  params,
}: {
  params: Promise<{ paramName: string }>;
}) {
  const { paramName } = use(params);
```

**Per-file specifics:**

**post/[postId]/page.tsx:**
- Remove `useParams` from `import { useParams, useRouter } from 'next/navigation'` (keep useRouter if used)
- Add `import { use } from 'react'` (or add `use` to existing react import)
- Change signature to accept `params: Promise<{ postId: string }>`
- Replace `const params = useParams(); const postId = params.postId as string;` with `const { postId } = use(params);`

**item/[itemId]/page.tsx:**
- Remove `useParams` from `import { useParams, useRouter } from 'next/navigation'` (keep useRouter if used)
- Add `use` to react import
- Change signature to accept `params: Promise<{ itemId: string }>`
- Replace `const params = useParams<{ itemId: string }>();` with `const { itemId } = use(params);`

**collection/[categorySlug]/page.tsx:**
- Remove `import { useParams } from 'next/navigation'` entirely (or keep if other imports exist)
- Add `use` to react import
- Change signature to accept `params: Promise<{ categorySlug: string }>`
- Replace `const params = useParams<{ categorySlug: string }>();` with `const { categorySlug } = use(params);`

**collection/[categorySlug]/[seriesSlug]/page.tsx:**
- Remove `import { useParams } from 'next/navigation'` entirely (or keep if other imports exist)
- Add `use` to react import
- Change signature to accept `params: Promise<{ categorySlug: string; seriesSlug: string }>`
- Replace `const params = useParams<{ categorySlug: string; seriesSlug: string }>();` with `const { categorySlug, seriesSlug } = use(params);`

**KEEP** all existing component logic, hooks, JSX, and other imports unchanged. Only the params access pattern changes.

After all 4 files are migrated, verify:
```bash
cd frontend && pnpm build
```
  </action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/Figly/frontend && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - post/[postId]/page.tsx contains `use(params)` and does NOT contain `useParams`
    - post/[postId]/page.tsx contains `params: Promise<{ postId: string }>`
    - item/[itemId]/page.tsx contains `use(params)` and does NOT contain `useParams`
    - item/[itemId]/page.tsx contains `params: Promise<{ itemId: string }>`
    - collection/[categorySlug]/page.tsx contains `use(params)` and does NOT contain `useParams`
    - collection/[categorySlug]/page.tsx contains `params: Promise<{ categorySlug: string }>`
    - collection/[categorySlug]/[seriesSlug]/page.tsx contains `use(params)` and does NOT contain `useParams`
    - collection/[categorySlug]/[seriesSlug]/page.tsx contains `params: Promise<{ categorySlug: string; seriesSlug: string }>`
    - `grep -r "useParams" frontend/src/app/` returns 0 matches
    - `cd frontend && pnpm build` exits 0
  </acceptance_criteria>
  <done>All 4 pages migrated from useParams() to async params with use() hook. Zero useParams calls remain in the codebase. Frontend builds successfully.</done>
</task>

</tasks>

<verification>
- `grep -r "useParams" frontend/src/app/` returns 0 matches
- `cd frontend && pnpm build` exits 0
- All 4 page files contain `use(params)` pattern
- All 4 page files have Promise-typed params prop in function signature
</verification>

<success_criteria>
All 4 remaining pages migrated from useParams() to the Next.js 16 async params pattern with use() hook. No useParams calls remain in page components. Frontend compiles and builds without errors.
</success_criteria>

<output>
After completion, create `.planning/phases/12-framework-upgrades/12-03-SUMMARY.md`
</output>
