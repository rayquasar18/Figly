---
phase: 03-content-feed
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - backend/src/posts/posts.service.ts
  - backend/src/posts/posts.controller.ts
  - backend/src/posts/posts.module.ts
  - backend/src/posts/dto/create-post.dto.ts
  - backend/src/posts/dto/update-post.dto.ts
  - backend/src/posts/__tests__/posts.service.spec.ts
  - backend/src/comments/comments.service.ts
  - backend/src/comments/comments.controller.ts
  - backend/src/comments/comments.module.ts
  - backend/src/comments/dto/create-comment.dto.ts
  - backend/src/comments/__tests__/comments.service.spec.ts
  - backend/src/feed/feed.service.ts
  - backend/src/feed/feed.controller.ts
  - backend/src/feed/feed.module.ts
  - backend/src/feed/__tests__/feed.service.spec.ts
  - backend/src/profiles/profiles.controller.ts
  - backend/src/profiles/profiles.service.ts
  - backend/src/app.module.ts
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-04
  - CONT-05
  - CONT-06
  - INTR-01
  - INTR-02
  - INTR-03
  - INTR-04
  - INTR-05
  - SOCL-03

must_haves:
  truths:
    - "POST /posts creates a post with caption and mediaIds, extracting hashtags from caption"
    - "GET /posts/:id returns full post detail with media URLs, like/bookmark status, counts"
    - "PATCH /posts/:id updates caption (owner only), re-extracting hashtags"
    - "DELETE /posts/:id deletes post with cascade (owner only)"
    - "POST/DELETE /posts/:id/like toggles like with P2002/P2025 idempotency"
    - "POST/DELETE /posts/:id/bookmark toggles bookmark with P2002/P2025 idempotency"
    - "GET /posts/saved returns bookmarked posts with cursor pagination"
    - "GET /posts/user/:username returns user posts for profile grid"
    - "POST /posts/:id/comments creates comment or reply (enforcing 1-level depth)"
    - "GET /posts/:id/comments returns top-level comments with eager-loaded replies"
    - "GET /feed returns chronological posts from followed users + own posts"
    - "GET /hashtags/search returns hashtag autocomplete results"
    - "GET /profiles/search returns user autocomplete results for @mentions"
  artifacts:
    - path: "backend/src/posts/posts.service.ts"
      provides: "Post CRUD, like/bookmark toggle, saved posts, user posts"
      exports: ["PostsService"]
    - path: "backend/src/posts/posts.controller.ts"
      provides: "REST endpoints for posts and interactions"
      exports: ["PostsController"]
    - path: "backend/src/comments/comments.service.ts"
      provides: "Comment CRUD with threading enforcement"
      exports: ["CommentsService"]
    - path: "backend/src/comments/comments.controller.ts"
      provides: "REST endpoints for comments"
      exports: ["CommentsController"]
    - path: "backend/src/feed/feed.service.ts"
      provides: "Chronological feed query with batch like/bookmark check"
      exports: ["FeedService"]
    - path: "backend/src/feed/feed.controller.ts"
      provides: "GET /feed endpoint"
      exports: ["FeedController"]
    - path: "backend/src/posts/__tests__/posts.service.spec.ts"
      provides: "Unit tests for PostsService"
    - path: "backend/src/comments/__tests__/comments.service.spec.ts"
      provides: "Unit tests for CommentsService"
    - path: "backend/src/feed/__tests__/feed.service.spec.ts"
      provides: "Unit tests for FeedService"
  key_links:
    - from: "backend/src/posts/posts.service.ts"
      to: "prisma.post"
      via: "Prisma client for Post CRUD"
      pattern: "prisma\\.post\\.(create|findUnique|update|delete|findMany)"
    - from: "backend/src/feed/feed.service.ts"
      to: "prisma.post.findMany with Follow subquery"
      via: "Feed query using user.followers.some"
      pattern: "followers.*some.*followerId"
    - from: "backend/src/posts/posts.service.ts"
      to: "backend/src/media/storage.service.ts"
      via: "Presigned URL generation for post images"
      pattern: "storageService\\.getPresignedUrl"
    - from: "backend/src/comments/comments.service.ts"
      to: "prisma.comment"
      via: "Comment CRUD with parent depth enforcement"
      pattern: "prisma\\.comment\\.(create|findMany|delete)"
---

<objective>
Build the complete backend API for posts, comments, interactions (like/bookmark), feed, and search autocomplete. Three NestJS modules (PostsModule, CommentsModule, FeedModule) following established patterns from Phase 1-2.

Purpose: Provide all REST endpoints the frontend needs for post creation, interactions, feed browsing, and autocomplete.
Output: 3 new NestJS modules with controllers, services, DTOs, and unit tests. Profile search endpoint added to existing ProfilesController.
</objective>

<execution_context>
@/Users/mqcbook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mqcbook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-content-feed/03-CONTEXT.md
@.planning/phases/03-content-feed/03-RESEARCH.md
@.planning/phases/03-content-feed/03-01-SUMMARY.md

<interfaces>
<!-- Types created in Plan 03-01 that this plan implements against -->

From packages/shared/src/dto/post.dto.ts:
```typescript
export const createPostSchema = z.object({
  mediaIds: z.array(z.string()).min(1).max(10),
  caption: z.string().max(2200).optional().nullable(),
});
export type CreatePostDto = z.infer<typeof createPostSchema>;

export const updateCaptionSchema = z.object({
  caption: z.string().max(2200).optional().nullable(),
});
export type UpdateCaptionDto = z.infer<typeof updateCaptionSchema>;
```

From packages/shared/src/dto/comment.dto.ts:
```typescript
export const createCommentSchema = z.object({
  content: z.string().min(1).max(1000),
  parentId: z.string().optional().nullable(),
});
export type CreateCommentDto = z.infer<typeof createCommentSchema>;
```

From packages/shared/src/types/post.types.ts:
```typescript
export interface PostMediaItem { id: string; mediaId: string; position: number; url: string; }
export interface PostAuthor { id: string; username: string; displayName: string; avatarUrl: string | null; }
export interface PostResponse { id: string; author: PostAuthor; caption: string | null; media: PostMediaItem[]; likeCount: number; commentCount: number; isLiked: boolean; isBookmarked: boolean; createdAt: string; updatedAt: string; }
export type FeedPostResponse = PostResponse;
```

From packages/shared/src/types/comment.types.ts:
```typescript
export interface CommentAuthor { id: string; username: string; displayName: string; avatarUrl: string | null; }
export interface CommentResponse { id: string; author: CommentAuthor; content: string; parentId: string | null; replies: CommentResponse[]; createdAt: string; }
```

From packages/shared/src/constants/index.ts:
```typescript
export const POST_LIMITS = { maxImages: 10, captionMaxLength: 2200, commentMaxLength: 1000, feedPageSize: 10, commentsPageSize: 20 } as const;
```

<!-- Existing backend patterns to follow -->

From backend/src/profiles/profiles.service.ts (established patterns):
- PrismaService injection via constructor
- StorageService injection for presigned URLs
- Cursor pagination with take+1 pattern
- Batch relationship checking with IN clause + Set

From backend/src/social/social.service.ts (established patterns):
- P2002/P2025 idempotent toggle pattern
- JwtAuthGuard + EmailVerifiedGuard on write endpoints
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PostsModule -- post CRUD, like/bookmark toggle, saved posts, hashtag search</name>
  <files>backend/src/posts/posts.service.ts, backend/src/posts/posts.controller.ts, backend/src/posts/posts.module.ts, backend/src/posts/dto/create-post.dto.ts, backend/src/posts/dto/update-post.dto.ts, backend/src/posts/__tests__/posts.service.spec.ts, backend/src/app.module.ts</files>
  <behavior>
    - createPost: creates post with caption + mediaIds, validates all mediaIds belong to user and are COMPLETED, creates PostMedia entries with position ordering, extracts hashtags with Unicode regex /#([\p{L}\p{N}_]+)/gu and upserts Hashtag + PostHashtag records in transaction
    - createPost: rejects if any mediaId invalid/not owned/not COMPLETED (BadRequestException, Vietnamese message)
    - createPost: rejects if mediaIds count exceeds 10 (validated by DTO)
    - getPost: returns PostResponse with media (presigned URLs via StorageService), author (avatar presigned URL), likeCount, commentCount, isLiked, isBookmarked for the requesting user
    - getPost: throws NotFoundException if post does not exist
    - updateCaption: only owner can update, re-extracts hashtags (delete old PostHashtag records, upsert new ones)
    - updateCaption: throws ForbiddenException if not owner
    - deletePost: only owner can delete, cascade removes all related records
    - deletePost: throws ForbiddenException if not owner
    - toggleLike(true): creates Like, returns success. If P2002 (already liked), returns success (idempotent)
    - toggleLike(false): deletes Like, returns success. If P2025 (not found), returns success (idempotent)
    - toggleBookmark(true/false): same P2002/P2025 idempotent pattern as like
    - getSavedPosts: returns bookmarked posts for user with cursor pagination (take+1 pattern), ordered by bookmark createdAt DESC
    - getUserPosts: returns posts by username with cursor pagination for profile grid
    - searchHashtags: returns hashtags matching prefix query (case-insensitive, limit 10)
  </behavior>
  <action>
**TDD RED phase:** Create `backend/src/posts/__tests__/posts.service.spec.ts` with tests for all behaviors listed above. Mock PrismaService and StorageService. Run tests -- they must fail (PostsService doesn't exist yet).

**TDD GREEN phase:** Implement:

1. **backend/src/posts/dto/create-post.dto.ts**: class-validator DTO matching createPostSchema. Use @IsArray, @ArrayMinSize(1), @ArrayMaxSize(10), @IsString({each: true}) for mediaIds. @IsOptional, @MaxLength(2200) for caption. Vietnamese error messages.

2. **backend/src/posts/dto/update-post.dto.ts**: class-validator DTO matching updateCaptionSchema. @IsOptional, @MaxLength(2200) for caption.

3. **backend/src/posts/posts.service.ts**: Inject PrismaService and StorageService.
   - `createPost(userId, dto)`: Validate mediaIds belong to user with COMPLETED status. Extract hashtags. Transaction: create Post + PostMedia entries + upsert Hashtags + create PostHashtags. Return post ID.
   - `getPost(postId, viewerId)`: findUnique with includes (user with avatar, media ordered by position, _count for likes/comments). Batch check isLiked/isBookmarked. Resolve presigned URLs for all media items AND author avatar. Map to PostResponse.
   - `updateCaption(postId, userId, dto)`: Check ownership. Update caption. Delete old PostHashtags, re-extract and upsert new hashtags in transaction.
   - `deletePost(postId, userId)`: Check ownership. Delete post (cascades handle rest).
   - `toggleLike(userId, postId, like)`: P2002/P2025 idempotent pattern (same as social follow/unfollow).
   - `toggleBookmark(userId, postId, bookmark)`: Same P2002/P2025 pattern.
   - `getSavedPosts(userId, cursor, take)`: Query bookmarks with post include, cursor pagination. Resolve presigned URLs. Return PaginatedResponse.
   - `getUserPosts(username, viewerId, cursor, take)`: Query posts by username, cursor pagination. Batch like/bookmark check. Resolve URLs. Return PaginatedResponse.
   - `searchHashtags(query, limit)`: findMany on Hashtag where name startsWith query.toLowerCase(), take limit.
   - Private `extractHashtags(text)`: Unicode regex `/#([\p{L}\p{N}_]+)/gu`, deduplicate, lowercase.
   - Private helper to resolve presigned URLs for a batch of posts (collect all storage keys, Promise.all for URLs, map back). Avoid N+1 per-post per-image URL generation.

4. **backend/src/posts/posts.controller.ts**: All endpoints use @UseGuards(JwtAuthGuard, EmailVerifiedGuard) except searchHashtags (JwtAuthGuard only).
   - `POST /posts` -- createPost
   - `GET /posts/saved` -- getSavedPosts (MUST be before :id route to avoid conflict)
   - `GET /posts/user/:username` -- getUserPosts
   - `GET /posts/:id` -- getPost
   - `PATCH /posts/:id` -- updateCaption
   - `DELETE /posts/:id` -- deletePost
   - `POST /posts/:id/like` -- like (HttpCode 200)
   - `DELETE /posts/:id/like` -- unlike (HttpCode 200)
   - `POST /posts/:id/bookmark` -- bookmark (HttpCode 200)
   - `DELETE /posts/:id/bookmark` -- unbookmark (HttpCode 200)
   - `GET /hashtags/search?q=` -- searchHashtags (query param)

   IMPORTANT: Static routes (saved, user/:username) MUST be declared BEFORE parameterized :id routes in the controller to avoid NestJS routing conflicts.

5. **backend/src/posts/posts.module.ts**: Import MediaModule (for StorageService). Register PostsService, PostsController.

6. **backend/src/app.module.ts**: Add PostsModule to imports.

Run all tests. All must pass.
  </action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/Figly/backend && pnpm test -- --testPathPattern=posts.service.spec --bail</automated>
  </verify>
  <done>PostsModule fully functional: create post (single + carousel), get post detail, update caption, delete post, like/unlike, bookmark/unbookmark, saved posts, user posts, hashtag search. All tests pass. Registered in AppModule.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CommentsModule, FeedModule, and profile search endpoint</name>
  <files>backend/src/comments/comments.service.ts, backend/src/comments/comments.controller.ts, backend/src/comments/comments.module.ts, backend/src/comments/dto/create-comment.dto.ts, backend/src/comments/__tests__/comments.service.spec.ts, backend/src/feed/feed.service.ts, backend/src/feed/feed.controller.ts, backend/src/feed/feed.module.ts, backend/src/feed/__tests__/feed.service.spec.ts, backend/src/profiles/profiles.controller.ts, backend/src/profiles/profiles.service.ts, backend/src/app.module.ts</files>
  <behavior>
    - createComment: creates top-level comment (parentId null) on a post
    - createComment with parentId: creates reply. If parent is itself a reply (parent.parentId is not null), flatten by using parent's parentId as the new comment's parentId (enforce 1-level depth like Instagram)
    - createComment: throws NotFoundException if post or parent comment doesn't exist
    - getComments: returns top-level comments for a post (parentId is null) ordered by createdAt ASC (oldest first), cursor paginated (20 per page). Each comment eagerly includes replies ordered by createdAt ASC.
    - deleteComment: only comment owner can delete
    - getFeed: returns chronological posts from followed users + own posts, newest first, cursor paginated (10 per page). Batch checks isLiked/isBookmarked for viewer. Resolves presigned URLs for all post media and author avatars.
    - getFeed: returns empty result for user following nobody (only own posts)
    - searchProfiles: returns users matching username or name prefix query (case-insensitive, limit 10) for @mention autocomplete
  </behavior>
  <action>
**TDD RED phase:** Create test files:
- `backend/src/comments/__tests__/comments.service.spec.ts`: Tests for createComment (top-level), createComment (reply), createComment (reply-to-reply flattens), getComments (paginated with replies), deleteComment (owner), deleteComment (non-owner forbidden).
- `backend/src/feed/__tests__/feed.service.spec.ts`: Tests for getFeed (returns followed + own posts in chronological order), getFeed (cursor pagination), getFeed (batch like/bookmark status), getFeed (empty when following nobody except own posts).

Run tests -- they must fail.

**TDD GREEN phase:** Implement:

1. **backend/src/comments/dto/create-comment.dto.ts**: class-validator DTO. @IsString, @MinLength(1), @MaxLength(1000) for content. @IsOptional, @IsString for parentId. Vietnamese messages.

2. **backend/src/comments/comments.service.ts**: Inject PrismaService and StorageService.
   - `createComment(userId, postId, dto)`: Verify post exists. If parentId provided, fetch parent comment. If parent.parentId is not null (parent is a reply), set parentId = parent.parentId (flatten to 1 level). Create comment. Return CommentResponse.
   - `getComments(postId, cursor?, take = 20)`: findMany where postId AND parentId is null, ordered by createdAt ASC, cursor pagination (take+1). Include: user (with avatar), replies (include user with avatar, ordered by createdAt ASC). Resolve avatar presigned URLs. Return PaginatedResponse<CommentResponse>.
   - `deleteComment(commentId, userId)`: Check ownership (ForbiddenException if not owner). Delete comment (cascade handles replies).

3. **backend/src/comments/comments.controller.ts**: All endpoints use @UseGuards(JwtAuthGuard, EmailVerifiedGuard).
   - `GET /posts/:postId/comments` -- getComments (query params: cursor, take)
   - `POST /posts/:postId/comments` -- createComment
   - `DELETE /comments/:id` -- deleteComment

4. **backend/src/comments/comments.module.ts**: Import MediaModule for StorageService.

5. **backend/src/feed/feed.service.ts**: Inject PrismaService and StorageService.
   - `getFeed(userId, cursor?, take = 10)`: Query posts WHERE userId = viewer OR user.followers.some(followerId = viewer), ordered by createdAt DESC, cursor pagination (take+1). Include user (with avatar), media (ordered by position), _count (likes, comments). Batch check isLiked/isBookmarked with IN clause + Set. Batch resolve presigned URLs. Return PaginatedResponse<FeedPostResponse>.
   - Use the exact feed query pattern from 03-RESEARCH.md Pattern 2.

6. **backend/src/feed/feed.controller.ts**: @UseGuards(JwtAuthGuard, EmailVerifiedGuard).
   - `GET /feed` -- getFeed (query params: cursor)

7. **backend/src/feed/feed.module.ts**: Import MediaModule for StorageService.

8. **backend/src/profiles/profiles.service.ts**: Add `searchProfiles(query, limit = 10)` method. Query users WHERE username contains query (case-insensitive) OR name contains query (case-insensitive), take limit. Return array of { id, username, displayName, avatarUrl }.

9. **backend/src/profiles/profiles.controller.ts**: Add `GET /profiles/search?q=` endpoint (JwtAuthGuard only, no EmailVerifiedGuard needed for search). Place BEFORE :username route to avoid conflict.

10. **backend/src/app.module.ts**: Add CommentsModule and FeedModule to imports.

Run all tests. All must pass including existing tests.
  </action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/Figly/backend && pnpm test --bail</automated>
  </verify>
  <done>CommentsModule (create, list with threading, delete), FeedModule (chronological feed with batch status), and profile search endpoint all functional. All unit tests pass (existing + new). All 3 modules registered in AppModule.</done>
</task>

</tasks>

<verification>
1. `cd backend && pnpm test --bail` -- all tests pass (existing + new posts/comments/feed tests)
2. `pnpm turbo build` -- all workspaces build
3. API endpoints match the design from 03-RESEARCH.md
</verification>

<success_criteria>
- POST /posts creates single-image and carousel posts with hashtag extraction
- GET /posts/:id returns full post detail with resolved presigned URLs
- PATCH /posts/:id updates caption (owner only) with hashtag re-extraction
- DELETE /posts/:id cascade deletes (owner only)
- Like and bookmark toggle endpoints are idempotent (P2002/P2025)
- GET /posts/saved returns bookmarked posts with cursor pagination
- GET /posts/user/:username returns user posts for profile grid
- GET /posts/:postId/comments returns threaded comments (1-level deep) with pagination
- POST /posts/:postId/comments creates comments with reply flattening
- GET /feed returns chronological posts from followed users + own posts
- GET /hashtags/search and GET /profiles/search provide autocomplete
- All unit tests pass, all workspaces build
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-feed/03-02-SUMMARY.md`
</output>
