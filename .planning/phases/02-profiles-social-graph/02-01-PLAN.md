---
phase: 02-profiles-social-graph
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/prisma/schema.prisma
  - packages/shared/src/validators/username.ts
  - packages/shared/src/types/profile.types.ts
  - packages/shared/src/types/user.types.ts
  - packages/shared/src/dto/profile.dto.ts
  - packages/shared/src/index.ts
  - packages/shared/src/constants/index.ts
  - backend/src/profiles/profiles.module.ts
  - backend/src/profiles/profiles.controller.ts
  - backend/src/profiles/profiles.service.ts
  - backend/src/profiles/dto/update-profile.dto.ts
  - backend/src/profiles/__tests__/profiles.service.spec.ts
  - backend/src/social/social.module.ts
  - backend/src/social/social.controller.ts
  - backend/src/social/social.service.ts
  - backend/src/social/__tests__/social.service.spec.ts
  - backend/src/app.module.ts
  - backend/src/auth/auth.service.ts
  - backend/src/auth/auth.controller.ts
  - backend/src/auth/dto/auth.dto.ts
autonomous: true
requirements:
  - PROF-01
  - PROF-02
  - PROF-03
  - SOCL-01
  - SOCL-02

must_haves:
  truths:
    - "GET /profiles/:username returns profile data with follower/following counts and relationship status"
    - "PATCH /profiles/me updates display name, bio, avatar, and username with cooldown enforcement"
    - "POST /social/follow/:userId creates a follow relationship"
    - "DELETE /social/follow/:userId removes a follow relationship"
    - "GET /social/:username/followers returns paginated follower list with search filter"
    - "GET /social/:username/following returns paginated following list with search filter"
    - "DELETE /social/followers/:userId removes a follower silently"
    - "Signup accepts username field and validates uniqueness"
    - "Username validation enforces 3-30 chars, lowercase+numbers+underscores+periods"
  artifacts:
    - path: "backend/prisma/schema.prisma"
      provides: "User model with username, bio, avatarId, usernameChangedAt fields; Follow model with explicit join table"
      contains: "model Follow"
    - path: "backend/src/profiles/profiles.service.ts"
      provides: "Profile CRUD with username cooldown, avatar URL resolution"
      exports: ["ProfilesService"]
    - path: "backend/src/social/social.service.ts"
      provides: "Follow/unfollow logic, follower/following lists with cursor pagination"
      exports: ["SocialService"]
    - path: "packages/shared/src/types/profile.types.ts"
      provides: "ProfileResponse, UserListItem, PaginatedResponse types"
      exports: ["ProfileResponse", "UserListItem", "PaginatedResponse"]
    - path: "packages/shared/src/validators/username.ts"
      provides: "Username validation rules and Zod schema"
      exports: ["USERNAME_RULES", "usernameSchema", "bioSchema"]
  key_links:
    - from: "backend/src/profiles/profiles.service.ts"
      to: "prisma.user"
      via: "Prisma queries with _count for follower/following counts"
      pattern: "_count.*followers.*following"
    - from: "backend/src/social/social.service.ts"
      to: "prisma.follow"
      via: "Prisma CRUD on Follow model"
      pattern: "prisma\\.follow\\.(create|delete|findMany)"
    - from: "backend/src/profiles/profiles.service.ts"
      to: "backend/src/media/storage.service.ts"
      via: "Presigned URL generation for avatar"
      pattern: "getPresignedUrl"
    - from: "backend/src/auth/auth.service.ts"
      to: "packages/shared/src/validators/username.ts"
      via: "Username validation during signup"
      pattern: "username"
---

<objective>
Build the complete backend for profiles and social graph: extend Prisma schema with username/bio/avatar fields and Follow model, create profiles module (get/update profile, username check), create social module (follow/unfollow, follower/following lists with cursor pagination and search), and modify signup to collect @username.

Purpose: Provides all API endpoints that the frontend profile pages and follow UI will consume in Plans 02 and 03.
Output: Two new NestJS modules (profiles, social) with full test coverage, extended Prisma schema, shared types/validators.
</objective>

<execution_context>
@/Users/mqcbook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mqcbook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-profiles-social-graph/02-RESEARCH.md
@.planning/phases/02-profiles-social-graph/02-CONTEXT.md
@.planning/phases/01-foundation-auth/01-01-SUMMARY.md
@.planning/phases/01-foundation-auth/01-02-SUMMARY.md
@.planning/phases/01-foundation-auth/01-03-SUMMARY.md

<interfaces>
<!-- Existing contracts the executor needs -->

From backend/prisma/schema.prisma (current User model to extend):
```prisma
model User {
  id              String   @id @default(cuid())
  email           String   @unique
  passwordHash    String?
  name            String
  emailVerified   Boolean  @default(false)
  emailVerifiedAt DateTime?
  googleId        String?  @unique
  appleId         String?  @unique
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
  refreshTokens      RefreshToken[]
  verificationTokens VerificationToken[]
  passwordResetTokens PasswordResetToken[]
  media              Media[]
  @@map("users")
}

model Media {
  id           String      @id @default(cuid())
  userId       String
  user         User        @relation(fields: [userId], references: [id], onDelete: Cascade)
  originalKey  String
  thumbnailKey String?
  mediumKey    String?
  largeKey     String?
  mimeType     String
  size         Int
  status       MediaStatus @default(PROCESSING)
  createdAt    DateTime    @default(now())
  @@index([userId])
  @@map("media")
}
```

From packages/shared/src/types/user.types.ts:
```typescript
export interface PublicUser {
  id: string;
  email: string;
  name: string;
  emailVerified: boolean;
}
```

From backend/src/auth/auth.service.ts:
```typescript
async signup(dto: { email: string; password: string; name: string })
async getMe(userId: string)
```

From backend/src/auth/auth.controller.ts:
```typescript
@Post('signup')
async signup(@Body() signupDto: SignupDto)
```

From backend/src/media/storage.service.ts (for presigned URLs):
```typescript
// StorageService provides getPresignedUrl(key: string): Promise<string>
```

From packages/shared/src/constants/index.ts:
```typescript
export const TOKEN_EXPIRY = { ... };
export const FILE_LIMITS = { ... };
export const THUMBNAIL_SIZES = { ... };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema extension, shared types/validators, and signup username support</name>
  <files>
    backend/prisma/schema.prisma,
    packages/shared/src/validators/username.ts,
    packages/shared/src/types/profile.types.ts,
    packages/shared/src/types/user.types.ts,
    packages/shared/src/dto/profile.dto.ts,
    packages/shared/src/constants/index.ts,
    packages/shared/src/index.ts,
    backend/src/auth/auth.service.ts,
    backend/src/auth/auth.controller.ts,
    backend/src/auth/dto/auth.dto.ts
  </files>
  <behavior>
    - Username schema validates: min 3, max 30, lowercase+numbers+underscores+periods only
    - Username schema rejects: uppercase, spaces, special chars, too short, too long
    - Bio schema validates: max 150 chars, optional/nullable
    - Reserved usernames (api, auth, admin, login, signup, settings, explore, search, help, about, terms, privacy, notifications, messages, feed, discover) are rejected
    - Signup DTO accepts username field and validates it
    - Signup service stores username in User record
  </behavior>
  <action>
    1. Extend Prisma schema:
       - Add to User model: `username String @unique`, `bio String? @db.VarChar(150)`, `avatarId String?`, `usernameChangedAt DateTime?`
       - Add avatar relation: `avatar Media? @relation("UserAvatar", fields: [avatarId], references: [id])`
       - Add self-referential follow relations: `followers Follow[] @relation("following")`, `following Follow[] @relation("follower")`
       - Add Media reverse relation for UserAvatar: add `userAvatar User? @relation("UserAvatar")` to Media model
       - Create Follow model with explicit join table: id (cuid), followerId, followingId, createdAt. @@unique([followerId, followingId]), @@index on both FKs, @@map("follows"), onDelete: Cascade on both relations
       - Run `cd backend && npx prisma generate` (do NOT run db push -- user needs Docker running)

    2. Create shared package types and validators:
       - `packages/shared/src/validators/username.ts`: USERNAME_RULES constant (minLength: 3, maxLength: 30, pattern: /^[a-z0-9_.]+$/, cooldownDays: 14), RESERVED_USERNAMES array, usernameSchema (Zod with Vietnamese messages: "Ten nguoi dung phai co it nhat 3 ky tu", "Ten nguoi dung khong duoc vuot qua 30 ky tu", "Ten nguoi dung chi chua chu thuong, so, dau gach duoi va dau cham"), bioSchema (max 150, Vietnamese message)
       - `packages/shared/src/types/profile.types.ts`: ProfileResponse interface (id, username, displayName, bio, avatarUrl, postCount, followerCount, followingCount, isOwnProfile, isFollowing, isFollowedBy), UserListItem interface (id, username, displayName, avatarUrl, isFollowing), PaginatedResponse<T> generic (items, nextCursor, hasMore)
       - `packages/shared/src/dto/profile.dto.ts`: updateProfileSchema (displayName optional string 1-50, username optional via usernameSchema, bio optional via bioSchema, avatarId optional string)
       - Update `packages/shared/src/types/user.types.ts`: Add username field to PublicUser interface
       - Add PROFILE_LIMITS to constants: { bioMaxLength: 150, usernameMinLength: 3, usernameMaxLength: 30, usernameCooldownDays: 14 }
       - Update `packages/shared/src/index.ts` barrel exports to include all new exports

    3. Modify signup to collect username:
       - Update `backend/src/auth/dto/auth.dto.ts`: Add username field to SignupDto with class-validator decorators (@IsString, @MinLength(3), @MaxLength(30), @Matches(/^[a-z0-9_.]+$/))
       - Update `backend/src/auth/auth.service.ts` signup method: Accept username in dto, check uniqueness (catch P2002), check against RESERVED_USERNAMES, store in User create data
       - Update `backend/src/auth/auth.controller.ts`: No changes needed (SignupDto already spreads)
       - Handle OAuth users without username: Update getMe to include username in response. Frontend will use null username to show "complete profile" interstitial (Plan 02).
       - Update getMe select to include username field
  </action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/Figly/backend && npx prisma validate && cd /Users/mqcbook/Documents/github/my-source/Figly && pnpm turbo build</automated>
  </verify>
  <done>
    Prisma schema has User with username/bio/avatarId/usernameChangedAt fields and Follow model. Shared package exports username/bio validators with Vietnamese messages, ProfileResponse/UserListItem/PaginatedResponse types, and updateProfileSchema. Signup DTO and service accept username. PublicUser includes username field. All builds pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Profiles and Social backend modules with tests</name>
  <files>
    backend/src/profiles/profiles.module.ts,
    backend/src/profiles/profiles.controller.ts,
    backend/src/profiles/profiles.service.ts,
    backend/src/profiles/dto/update-profile.dto.ts,
    backend/src/profiles/__tests__/profiles.service.spec.ts,
    backend/src/social/social.module.ts,
    backend/src/social/social.controller.ts,
    backend/src/social/social.service.ts,
    backend/src/social/__tests__/social.service.spec.ts,
    backend/src/app.module.ts
  </files>
  <behavior>
    - getProfile(username, viewerId) returns ProfileResponse with correct counts, isOwnProfile, isFollowing, isFollowedBy
    - getProfile throws NotFoundException for non-existent username
    - updateProfile validates username cooldown (14 days), uniqueness, reserved names
    - updateProfile updates usernameChangedAt only when username actually changes
    - isUsernameAvailable returns true/false correctly
    - follow(userId, targetUserId) creates Follow record, prevents self-follow, handles duplicate gracefully
    - unfollow(userId, targetUserId) deletes Follow record, handles non-existent gracefully
    - getFollowers returns paginated list with cursor, search filter, and viewer's follow status for each user
    - getFollowing returns paginated list with cursor, search filter, and viewer's follow status for each user
    - removeFollower(ownerId, followerUserId) deletes the follow relationship
  </behavior>
  <action>
    1. Create profiles module:
       - `backend/src/profiles/dto/update-profile.dto.ts`: NestJS DTO with class-validator decorators for displayName (optional, 1-50 chars), username (optional, matches pattern), bio (optional, max 150), avatarId (optional string). Vietnamese error messages.
       - `backend/src/profiles/profiles.service.ts`:
         - `getProfile(username: string, viewerId: string)`: Query user with `_count: { select: { followers: true, following: true } }`. Use `Promise.all` for isFollowing + isFollowedBy check (two findUnique on Follow with composite key). Resolve avatar presigned URL via StorageService if avatar exists. Return ProfileResponse shape. postCount hardcoded to 0 (Phase 3). Throw NotFoundException with Vietnamese message if user not found.
         - `updateProfile(userId: string, dto: UpdateProfileDto)`: Check username cooldown (14 days since usernameChangedAt), check uniqueness (catch P2002), check RESERVED_USERNAMES. Update only changed fields. Set usernameChangedAt when username changes. Return updated profile.
         - `isUsernameAvailable(username: string)`: Check against reserved list and database.
       - `backend/src/profiles/profiles.controller.ts`:
         - `GET /profiles/:username` (JwtAuthGuard) -- calls getProfile
         - `PATCH /profiles/me` (JwtAuthGuard, EmailVerifiedGuard) -- calls updateProfile
         - `GET /profiles/check/:username` -- calls isUsernameAvailable (public, for signup form)
       - `backend/src/profiles/profiles.module.ts`: Import PrismaModule. Import MediaModule for StorageService (or import StorageService directly if exported globally). Export ProfilesService.

    2. Create social module:
       - `backend/src/social/social.service.ts`:
         - `follow(userId: string, targetUserId: string)`: Prevent self-follow (BadRequestException). Create Follow record. Catch P2002 for duplicate (return success, idempotent).
         - `unfollow(userId: string, targetUserId: string)`: Delete Follow record. Catch not-found (return success, idempotent).
         - `getFollowers(username: string, viewerId: string, options: { cursor?, search?, take? })`: Find user by username. Query Follow where followingId = user.id. Apply search filter on follower name/username (case-insensitive contains). Cursor pagination with take+1 pattern. Batch follow-status check for viewer. Return PaginatedResponse<UserListItem>.
         - `getFollowing(username: string, viewerId: string, options)`: Same pattern but where followerId = user.id, select following user.
         - `removeFollower(ownerId: string, followerUserId: string)`: Delete Follow where followerId=followerUserId AND followingId=ownerId.
       - `backend/src/social/social.controller.ts`:
         - `POST /social/follow/:userId` (JwtAuthGuard, EmailVerifiedGuard)
         - `DELETE /social/follow/:userId` (JwtAuthGuard, EmailVerifiedGuard)
         - `GET /social/:username/followers` (JwtAuthGuard) with cursor and search query params
         - `GET /social/:username/following` (JwtAuthGuard) with cursor and search query params
         - `DELETE /social/followers/:userId` (JwtAuthGuard, EmailVerifiedGuard) -- remove follower
       - `backend/src/social/social.module.ts`: Import PrismaModule. Export SocialService.

    3. Register modules in app.module.ts: Add ProfilesModule and SocialModule to imports.

    4. Write unit tests (TDD -- write tests first, then implement):
       - `profiles.service.spec.ts`: Mock PrismaService. Test getProfile returns correct shape, handles not-found, computes isFollowing/isFollowedBy. Test updateProfile cooldown enforcement, uniqueness check, reserved username rejection. Test isUsernameAvailable.
       - `social.service.spec.ts`: Mock PrismaService. Test follow creates record, prevents self-follow, handles duplicate. Test unfollow deletes record, handles non-existent. Test getFollowers/getFollowing pagination and search. Test removeFollower.

    NOTE: Import StorageService from the media module for avatar URL resolution. If StorageService is not globally exported, import MediaModule in ProfilesModule. Check existing media module exports.
  </action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/Figly/backend && npx jest --testPathPattern="profiles|social" --no-coverage -x</automated>
  </verify>
  <done>
    ProfilesModule with GET /profiles/:username, PATCH /profiles/me, GET /profiles/check/:username endpoints working. SocialModule with POST/DELETE /social/follow/:userId, GET /social/:username/followers, GET /social/:username/following, DELETE /social/followers/:userId endpoints working. All unit tests pass. Modules registered in app.module.ts. Build passes.
  </done>
</task>

</tasks>

<verification>
1. `cd backend && npx prisma validate` -- schema is valid
2. `pnpm turbo build` -- all workspaces build
3. `cd backend && npx jest --testPathPattern="profiles|social" --no-coverage -x` -- all profile and social tests pass
4. `cd backend && npx jest --no-coverage` -- full test suite passes (including auth tests)
</verification>

<success_criteria>
- Prisma schema has username (unique, indexed), bio, avatarId, usernameChangedAt on User model and Follow model with composite unique constraint
- ProfilesService.getProfile returns ProfileResponse with counts and relationship flags
- ProfilesService.updateProfile enforces username cooldown and uniqueness
- SocialService.follow/unfollow/getFollowers/getFollowing/removeFollower all work correctly
- Shared package exports ProfileResponse, UserListItem, PaginatedResponse, usernameSchema, bioSchema, USERNAME_RULES
- Signup accepts username and stores it
- All tests pass, all workspaces build
</success_criteria>

<output>
After completion, create `.planning/phases/02-profiles-social-graph/02-01-SUMMARY.md`
</output>
