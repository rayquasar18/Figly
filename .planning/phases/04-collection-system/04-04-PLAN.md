---
phase: 04-collection-system
plan: 04
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - frontend/src/app/(app)/checklists/page.tsx
  - frontend/src/app/(app)/checklists/[checklistId]/page.tsx
  - frontend/src/app/(app)/checklists/new/page.tsx
  - frontend/src/components/checklist/checklist-card.tsx
  - frontend/src/components/checklist/checklist-entry.tsx
  - frontend/src/components/checklist/checklist-form.tsx
  - frontend/src/components/collection/item-picker.tsx
  - frontend/src/components/collection/collection-showcase.tsx
  - frontend/src/components/collection/follow-series-button.tsx
  - frontend/src/components/create-post/step-caption.tsx
  - frontend/src/components/profile/profile-header.tsx
  - frontend/src/components/post/post-card.tsx
  - frontend/src/components/post/post-detail-modal.tsx
  - frontend/src/hooks/queries/checklist-queries.ts
  - frontend/src/hooks/queries/collection-queries.ts
  - frontend/src/stores/create-post-store.ts
  - packages/shared/src/types/post.types.ts
  - packages/shared/src/dto/post.dto.ts
autonomous: true
requirements:
  - COLL-05
  - COLL-06
  - COLL-07
  - CONT-07
  - PROF-04
  - SOCL-04

must_haves:
  truths:
    - "User can create a named checklist, add database items or freeform entries, and see progress bar (X/Y complete)"
    - "User can check/uncheck entries in a checklist and see progress update"
    - "User can reorder checklist entries using up/down arrow buttons"
    - "When creating/editing a post, user can link items from the collection database via a search picker"
    - "Post detail shows linked items as tappable tags below the caption"
    - "Profile page has a Collection tab showing owned items organized by category"
    - "User can follow/unfollow a series or category from series and category pages"
  artifacts:
    - path: "frontend/src/app/(app)/checklists/page.tsx"
      provides: "User's checklists list with progress bars"
      min_lines: 30
    - path: "frontend/src/components/checklist/checklist-card.tsx"
      provides: "Checklist card with name and progress bar"
      min_lines: 20
    - path: "frontend/src/components/collection/item-picker.tsx"
      provides: "Search+select picker dialog for linking items to posts"
      min_lines: 50
    - path: "frontend/src/components/collection/collection-showcase.tsx"
      provides: "Profile collection tab content with owned items by category"
      min_lines: 40
    - path: "frontend/src/hooks/queries/checklist-queries.ts"
      provides: "TanStack Query hooks for checklist CRUD"
      exports: ["useMyChecklists", "useChecklistDetail", "useCreateChecklist", "useAddEntry", "useToggleEntry", "useReorderEntries"]
  key_links:
    - from: "frontend/src/hooks/queries/checklist-queries.ts"
      to: "/api/checklists/*"
      via: "apiClient calls"
      pattern: "apiClient\\.(get|post|patch|delete).*checklists"
    - from: "frontend/src/components/collection/item-picker.tsx"
      to: "collection-queries.ts"
      via: "useSearchItems hook for item selection"
      pattern: "useSearchItems"
    - from: "frontend/src/components/collection/collection-showcase.tsx"
      to: "collection-queries.ts"
      via: "API call for user owned items"
      pattern: "collection/users/.*/owned"
    - from: "frontend/src/components/create-post/step-caption.tsx"
      to: "item-picker.tsx"
      via: "ItemPicker dialog opened from caption step"
      pattern: "ItemPicker"
    - from: "frontend/src/components/profile/profile-header.tsx"
      to: "collection-showcase.tsx"
      via: "Tabs rendering Collection tab"
      pattern: "CollectionShowcase"
---

<objective>
Build frontend checklists (create, manage entries, track progress), post-to-item linking (item picker in create post flow + tags on post detail), profile collection showcase tab, and series/category follow buttons.

Purpose: Complete the remaining frontend features of the collection system -- checklists (COLL-05/06/07), post linking (CONT-07), profile showcase (PROF-04), and series following (SOCL-04).
Output: Checklist pages, item picker, collection showcase, follow buttons, extended post components
</objective>

<execution_context>
@/Users/mqcbook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mqcbook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-collection-system/04-CONTEXT.md
@.planning/phases/04-collection-system/04-RESEARCH.md
@.planning/phases/04-collection-system/04-01-SUMMARY.md
@.planning/phases/04-collection-system/04-02-SUMMARY.md

@frontend/src/components/create-post/step-caption.tsx
@frontend/src/components/create-post/create-post-flow.tsx
@frontend/src/stores/create-post-store.ts
@frontend/src/components/profile/profile-header.tsx
@frontend/src/components/post/post-card.tsx
@frontend/src/components/post/post-detail-modal.tsx
@frontend/src/hooks/queries/post-queries.ts
@frontend/src/hooks/queries/interaction-queries.ts
@packages/shared/src/types/checklist.types.ts
@packages/shared/src/types/collection.types.ts
@packages/shared/src/types/post.types.ts
@packages/shared/src/dto/checklist.dto.ts
@packages/shared/src/dto/post.dto.ts

<interfaces>
<!-- Types from Plan 01 -->
From packages/shared/src/types/checklist.types.ts:
```typescript
export interface ChecklistResponse {
  id: string; name: string; isPublic: boolean;
  totalEntries: number; checkedEntries: number;
  createdAt: string; updatedAt: string;
}
export interface ChecklistEntryResponse {
  id: string; checklistId: string; itemId: string | null;
  itemName: string | null; itemImageUrl: string | null;
  freeformText: string | null; isChecked: boolean; position: number;
}
export interface ChecklistDetailResponse extends ChecklistResponse {
  entries: ChecklistEntryResponse[];
}
```

From packages/shared/src/types/collection.types.ts:
```typescript
export interface LinkedItemResponse {
  id: string; name: string; seriesName: string;
  categoryName: string; imageUrl: string | null;
}
```

<!-- PostResponse extended in Plan 02 with linkedItems -->
From packages/shared/src/types/post.types.ts (extended):
```typescript
export interface PostResponse {
  // ...existing fields...
  linkedItems?: LinkedItemResponse[];
}
```

<!-- Existing create-post store pattern -->
From frontend/src/stores/create-post-store.ts:
```typescript
// Zustand store with: images, caption, step, setCaption, etc.
// Need to add: linkedItemIds: string[], addLinkedItem, removeLinkedItem
```

<!-- Existing profile header pattern -->
From frontend/src/components/profile/profile-header.tsx:
```typescript
// Renders: avatar, stats, bio, edit button
// Need to add: Tabs (Posts / Collection) wrapping the content below
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Checklist pages with CRUD, entries, and progress tracking</name>
  <files>
    frontend/src/hooks/queries/checklist-queries.ts,
    frontend/src/app/(app)/checklists/page.tsx,
    frontend/src/app/(app)/checklists/[checklistId]/page.tsx,
    frontend/src/app/(app)/checklists/new/page.tsx,
    frontend/src/components/checklist/checklist-card.tsx,
    frontend/src/components/checklist/checklist-entry.tsx,
    frontend/src/components/checklist/checklist-form.tsx
  </files>
  <action>
    **Checklist Query Hooks (frontend/src/hooks/queries/checklist-queries.ts):**
    - `useMyChecklists()`: useQuery GET /checklists -- returns user's checklists with counts
    - `useChecklistDetail(checklistId)`: useQuery GET /checklists/:id -- returns checklist with entries
    - `useCreateChecklist()`: useMutation POST /checklists -- invalidates checklists list on success
    - `useUpdateChecklist()`: useMutation PATCH /checklists/:id -- invalidates checklist detail + list
    - `useDeleteChecklist()`: useMutation DELETE /checklists/:id -- invalidates list, navigate to /checklists
    - `useAddEntry()`: useMutation POST /checklists/:id/entries -- invalidates checklist detail
    - `useToggleEntry()`: useMutation PATCH /checklists/entries/:id/toggle -- optimistic update on isChecked + progress counts
    - `useRemoveEntry()`: useMutation DELETE /checklists/entries/:id -- invalidates checklist detail
    - `useReorderEntries()`: useMutation PATCH /checklists/:id/reorder -- optimistic update on entry positions

    Toggle entry uses optimistic update: flip isChecked in cache, update totalEntries/checkedEntries counts.

    **ChecklistCard (frontend/src/components/checklist/checklist-card.tsx):**
    Display: checklist name, progress bar (shadcn Progress component), "X/Y hoan thanh" text, isPublic badge if public. Link to /checklists/:id. Created/updated date in relative time.

    **ChecklistEntry (frontend/src/components/checklist/checklist-entry.tsx):**
    Display: shadcn Checkbox for isChecked, item name or freeform text. If database item: show small image thumbnail + item name (linked to /item/:id). If freeform: show text. Up/down arrow buttons (ChevronUp, ChevronDown from lucide-react) for reordering. Delete button (Trash2 icon). Checked entries show line-through styling.

    **ChecklistForm (frontend/src/components/checklist/checklist-form.tsx):**
    Reusable form for create and edit: name input (max 100 chars), isPublic toggle (Switch component from shadcn), submit button. Uses react-hook-form with zodResolver and createChecklistSchema/updateChecklistSchema.

    **Checklists List Page (frontend/src/app/(app)/checklists/page.tsx):**
    - Title: "Checklist cua toi" (My Checklists)
    - Grid of ChecklistCards
    - "Tao checklist moi" (Create new checklist) button linking to /checklists/new
    - Empty state: "Ban chua co checklist nao" with CTA to create
    - This is an (app) route -- requires authentication

    **Create Checklist Page (frontend/src/app/(app)/checklists/new/page.tsx):**
    - Title: "Tao checklist moi"
    - ChecklistForm component
    - On success: navigate to /checklists/:newId

    **Checklist Detail Page (frontend/src/app/(app)/checklists/[checklistId]/page.tsx):**
    - Header: checklist name, edit button (opens inline edit or modal), delete button with confirmation
    - Progress bar with "X/Y hoan thanh" (X/Y complete)
    - List of ChecklistEntry components ordered by position
    - "Them muc" (Add entry) section at bottom: two options
      1. "Them tu database" button opens ItemPicker dialog (from Task 2) -- selects item, calls useAddEntry with itemId
      2. "Them tu do" (Add freeform) text input + add button -- calls useAddEntry with freeformText
    - Reorder: up/down buttons on each entry call useReorderEntries with recomputed order
    - Toggle: clicking checkbox calls useToggleEntry

    Reorder logic: when user clicks up arrow on entry at index N, swap with entry at index N-1, send the full array of entryIds in new order via useReorderEntries.
  </action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/Figly && npx tsc --noEmit -p frontend/tsconfig.json 2>&1 | head -30 || echo "TS check done"</automated>
  </verify>
  <done>Checklists page shows user's checklists with progress bars. Create flow works. Detail page shows entries with check/reorder/delete. Adding database items and freeform entries works. Progress updates optimistically on toggle.</done>
</task>

<task type="auto">
  <name>Task 2: Post-to-item linking, profile collection tab, and series/category follow</name>
  <files>
    frontend/src/components/collection/item-picker.tsx,
    frontend/src/components/collection/collection-showcase.tsx,
    frontend/src/components/collection/follow-series-button.tsx,
    frontend/src/components/create-post/step-caption.tsx,
    frontend/src/components/profile/profile-header.tsx,
    frontend/src/components/post/post-card.tsx,
    frontend/src/components/post/post-detail-modal.tsx,
    frontend/src/hooks/queries/collection-queries.ts,
    frontend/src/stores/create-post-store.ts,
    packages/shared/src/types/post.types.ts,
    packages/shared/src/dto/post.dto.ts
  </files>
  <action>
    **ItemPicker (frontend/src/components/collection/item-picker.tsx):**
    Dialog-based item picker for selecting collection items:
    - shadcn Dialog wrapping the picker UI
    - Search input with debounced search (300ms) using useSearchItems from collection-queries
    - Search results as a scrollable list of item rows (name, series, category, small image)
    - Click to select/deselect -- selected items shown as Badge chips below search
    - "Xong" (Done) button closes dialog and passes selected item IDs back via callback
    - Prop interface: `{ selectedItemIds: string[], onSelect: (ids: string[]) => void, open: boolean, onOpenChange: (open: boolean) => void }`

    **Extend create-post-store (frontend/src/stores/create-post-store.ts):**
    Add to the store:
    - `linkedItemIds: string[]` (default: [])
    - `linkedItems: LinkedItemResponse[]` (default: [] -- for display)
    - `addLinkedItem(item: LinkedItemResponse)`: adds to both arrays
    - `removeLinkedItem(itemId: string)`: removes from both arrays
    - `reset()`: also clears linkedItemIds and linkedItems

    **Extend StepCaption (frontend/src/components/create-post/step-caption.tsx):**
    Add below the caption textarea area:
    - "Lien ket vat pham" (Link items) button with Link icon that opens ItemPicker dialog
    - Below button: show selected items as removable Badge chips (item name + X to remove)
    - Clicking X calls removeLinkedItem from store
    - ItemPicker onSelect updates the store

    **Extend CreatePostFlow / post creation API call:**
    When submitting the post, include `linkedItemIds` from the store in the createPost API call body. The shared createPostSchema already has linkedItemIds (added in Plan 02 Task 2).

    **Extend PostCard (frontend/src/components/post/post-card.tsx):**
    If post has `linkedItems` and it's non-empty, render below the caption:
    - Row of tappable Badge components, each showing item name
    - Clicking a badge navigates to `/item/${itemId}`
    - Use Package icon prefix in each badge
    - Max 3 visible, "+N more" if more than 3

    **Extend PostDetailModal (frontend/src/components/post/post-detail-modal.tsx):**
    Same as PostCard but show all linked items (no limit), in a small section between caption and comments.

    **CollectionShowcase (frontend/src/components/collection/collection-showcase.tsx):**
    Profile tab content showing user's owned items:
    - Fetch from GET /collection/users/:username/owned
    - Group items by category name
    - Each category section: category name header + grid of item thumbnails (small ItemCard variant)
    - Empty state: "Chua co vat pham nao" (No items yet) with CTA to browse collection
    - For own profile: show owned count. For other profiles: same display if items exist

    **Profile Collection Tab (frontend/src/components/profile/profile-header.tsx):**
    Add shadcn Tabs around the profile content area. The profile page currently renders ProfilePostGrid directly below the header. Wrap it with:
    ```tsx
    <Tabs defaultValue="posts">
      <TabsList className="w-full justify-center border-b rounded-none bg-transparent">
        <TabsTrigger value="posts"><Grid3X3 className="size-4" /> Bai viet</TabsTrigger>
        <TabsTrigger value="collection"><Package className="size-4" /> Bo suu tap</TabsTrigger>
      </TabsList>
      <TabsContent value="posts"><ProfilePostGrid ... /></TabsContent>
      <TabsContent value="collection"><CollectionShowcase username={username} /></TabsContent>
    </Tabs>
    ```

    The Tabs integration may need to happen in the profile page.tsx rather than profile-header.tsx depending on how the current page is structured. Read the profile page first to determine the best insertion point.

    **FollowSeriesButton (frontend/src/components/collection/follow-series-button.tsx):**
    Reusable button for following/unfollowing a series or category:
    - Props: `{ type: 'series' | 'category', id: string, isFollowed: boolean }`
    - When followed: "Dang theo doi" (Following) with outline variant, hover shows "Bo theo doi" (Unfollow) with destructive styling -- same pattern as user FollowButton
    - When not followed: "Theo doi" (Follow) with default variant
    - On click: POST /collection/series/:id/follow or /collection/categories/:id/follow
    - Uses requireAuth pattern for unauthenticated users
    - Add follow mutations to collection-queries.ts: `useFollowSeries()`, `useFollowCategory()` with optimistic update

    **Add follow mutations to collection-queries.ts:**
    - `useFollowSeries()`: useMutation POST /collection/series/:id/follow, optimistic toggle on series detail queries
    - `useFollowCategory()`: useMutation POST /collection/categories/:id/follow, optimistic toggle on category detail queries
    - Integrate FollowSeriesButton into series page and category page (the pages created in Plan 03)

    Note: Plan 03 creates the series and category pages. This task adds the follow button to those pages. If Plan 03 runs in parallel (same wave), check if the files exist before modifying. If they don't exist yet (Plan 03 not complete), create a simple integration -- the follow button component is self-contained and can be imported by Plan 03's pages. Add a note in SUMMARY about where to place the follow button.
  </action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/Figly && npx tsc --noEmit -p frontend/tsconfig.json 2>&1 | head -30 || echo "TS check done"</automated>
  </verify>
  <done>ItemPicker dialog works with search and multi-select. StepCaption shows link items button and selected item chips. Posts created with linked items. PostCard and PostDetailModal show linked item badges. Profile has Collection tab with owned items by category. FollowSeriesButton renders with correct states.</done>
</task>

<task type="auto">
  <name>Task 3: Add checklists link to navigation and wire checklist item picker</name>
  <files>
    frontend/src/components/layout/bottom-nav.tsx,
    frontend/src/app/(app)/checklists/[checklistId]/page.tsx
  </files>
  <action>
    **BottomNav (frontend/src/components/layout/bottom-nav.tsx):**
    This file was already modified in Plan 03 to add the collection link. Now verify the nav has proper items. The nav should have: Home, Search/Explore, Create, Collection, Profile. If checklists need a separate nav entry, add it as a sub-route accessible from the collection page or profile. Per the CONTEXT.md, checklists are accessible from the profile collection showcase tab, so no separate bottom nav entry is needed. However, ensure `/checklists` is reachable from the profile collection tab by adding a "Xem checklist" (View checklists) link in the CollectionShowcase component.

    **Wire ItemPicker in Checklist Detail (frontend/src/app/(app)/checklists/[checklistId]/page.tsx):**
    Ensure the "Them tu database" button in the checklist detail page opens the ItemPicker dialog (created in Task 2 of this plan). When user selects an item from the picker, call useAddEntry with the selected itemId. The ItemPicker should be configured for single-select mode when adding to checklists (vs multi-select for post linking).

    Add a `mode: 'single' | 'multi'` prop to ItemPicker:
    - `single`: selecting an item immediately calls onSelect with [itemId] and closes
    - `multi`: selecting toggles, done button confirms

    Update ItemPicker component to support both modes.

    Also add a "Xem checklist" link in CollectionShowcase for the profile owner.
  </action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/Figly && npx tsc --noEmit -p frontend/tsconfig.json 2>&1 | head -30 || echo "TS check done"</automated>
  </verify>
  <done>Checklist detail page uses ItemPicker in single-select mode for adding database items. ItemPicker supports both single and multi mode. CollectionShowcase has link to checklists for profile owner. Navigation is clean and complete.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `cd frontend && npx tsc --noEmit`
2. Checklists CRUD flow works: create, add entries, check/uncheck, reorder, delete
3. Post creation includes item linking step, linked items appear on post
4. Profile has Collection tab with owned items by category
5. Follow buttons work on series/category pages
6. Full backend test suite still passes: `cd backend && npx jest --no-coverage`
</verification>

<success_criteria>
- Checklists: create named checklist, add database items + freeform entries, check/uncheck, see X/Y progress bar, reorder with up/down buttons
- Post linking: item picker in caption step, selected items as chips, linked items as badges on post card/detail
- Profile: Collection tab next to Posts tab, shows owned items grouped by category
- Follow: follow/unfollow button on series and category pages with optimistic UI
- Vietnamese text throughout
- Auth-aware: unauthenticated users redirected to login on interactive features
</success_criteria>

<output>
After completion, create `.planning/phases/04-collection-system/04-04-SUMMARY.md`
</output>
