---
phase: 02-profiles-social-graph
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - frontend/src/app/(app)/[username]/page.tsx
  - frontend/src/app/(app)/[username]/loading.tsx
  - frontend/src/components/profile/profile-header.tsx
  - frontend/src/components/profile/profile-stats.tsx
  - frontend/src/components/profile/profile-post-grid.tsx
  - frontend/src/components/profile/profile-edit-modal.tsx
  - frontend/src/components/profile/profile-skeleton.tsx
  - frontend/src/hooks/queries/profile-queries.ts
  - frontend/src/app/(auth)/signup/page.tsx
  - frontend/src/components/auth/signup-form.tsx
  - frontend/src/app/(app)/complete-profile/page.tsx
  - frontend/src/components/profile/complete-profile-form.tsx
  - frontend/src/app/(app)/layout.tsx
  - frontend/src/stores/auth-store.ts
autonomous: false
requirements:
  - PROF-01
  - PROF-02
  - PROF-03

must_haves:
  truths:
    - "User can visit /{username} and see Instagram-style profile with avatar, stats, bio, and post grid"
    - "User can tap 'Chinh sua trang ca nhan' on own profile and edit display name, username, bio, and avatar in a modal"
    - "User sees 'Theo doi ban' badge on profiles of users who follow them back"
    - "Signup form collects @username with real-time availability check"
    - "OAuth users without username see 'complete profile' interstitial before accessing the app"
    - "Post grid shows empty state with 'Chua co bai viet nao' message"
  artifacts:
    - path: "frontend/src/app/(app)/[username]/page.tsx"
      provides: "Profile page with dynamic username route"
      min_lines: 20
    - path: "frontend/src/components/profile/profile-header.tsx"
      provides: "Instagram-style header: circular avatar left, stats right, name+username+bio below"
      min_lines: 40
    - path: "frontend/src/components/profile/profile-edit-modal.tsx"
      provides: "Modal overlay for editing profile fields with avatar upload"
      min_lines: 60
    - path: "frontend/src/hooks/queries/profile-queries.ts"
      provides: "TanStack Query hooks for profile data and mutations"
      exports: ["useProfile", "useUpdateProfile", "useCheckUsername"]
    - path: "frontend/src/app/(app)/complete-profile/page.tsx"
      provides: "Username collection interstitial for OAuth users"
      min_lines: 15
  key_links:
    - from: "frontend/src/hooks/queries/profile-queries.ts"
      to: "/api/profiles/:username"
      via: "apiClient.get"
      pattern: "apiClient.*profiles"
    - from: "frontend/src/components/profile/profile-edit-modal.tsx"
      to: "/api/profiles/me"
      via: "useUpdateProfile mutation"
      pattern: "apiClient.*profiles/me"
    - from: "frontend/src/components/auth/signup-form.tsx"
      to: "/api/profiles/check/:username"
      via: "useCheckUsername query for availability"
      pattern: "profiles/check"
    - from: "frontend/src/app/(app)/layout.tsx"
      to: "frontend/src/app/(app)/complete-profile/page.tsx"
      via: "Redirect when user.username is null"
      pattern: "username.*complete-profile"
---

<objective>
Build the frontend profile experience: profile page at /[username] with Instagram-style header (avatar, stats, bio, post grid), edit profile modal with avatar upload, signup form update to collect @username with real-time availability check, and "complete profile" interstitial for existing OAuth users who lack a username.

Purpose: Delivers the visual profile identity that users see and interact with, fulfilling PROF-01/02/03.
Output: Profile page, edit modal, signup username field, complete-profile gate.
</objective>

<execution_context>
@/Users/mqcbook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mqcbook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-profiles-social-graph/02-RESEARCH.md
@.planning/phases/02-profiles-social-graph/02-CONTEXT.md
@.planning/phases/02-profiles-social-graph/02-02-SUMMARY.md

<interfaces>
<!-- Contracts from Plans 02-01 and 02-02 that this plan consumes -->

From packages/shared/src/types/profile.types.ts (created in 02-01):
```typescript
export interface ProfileResponse {
  id: string;
  username: string;
  displayName: string;
  bio: string | null;
  avatarUrl: string | null;
  postCount: number;
  followerCount: number;
  followingCount: number;
  isOwnProfile: boolean;
  isFollowing: boolean;
  isFollowedBy: boolean;
}
```

From packages/shared/src/validators/username.ts (created in 02-01):
```typescript
export const USERNAME_RULES = {
  minLength: 3,
  maxLength: 30,
  pattern: /^[a-z0-9_.]+$/,
  cooldownDays: 14,
};
export const usernameSchema: ZodString;
export const bioSchema: ZodOptional<ZodString>;
export const RESERVED_USERNAMES: string[];
```

From packages/shared/src/types/user.types.ts (updated in 02-01):
```typescript
export interface PublicUser {
  id: string;
  email: string;
  name: string;
  username: string | null;  // null for pre-Phase-2 OAuth users
  emailVerified: boolean;
}
```

From backend API endpoints (created in 02-01, implemented in 02-02):
- GET /profiles/:username -> ProfileResponse
- PATCH /profiles/me -> ProfileResponse (body: { displayName?, username?, bio?, avatarId? })
- GET /profiles/check/:username -> { available: boolean }
- POST /media/upload -> { id: string, status: 'PROCESSING' } (existing)

From frontend (existing):
```typescript
// frontend/src/lib/api-client.ts
export const apiClient: AxiosInstance;  // with auth interceptor

// frontend/src/stores/auth-store.ts
export const useAuthStore: StoreApi<{ user, setUser, clearUser }>;

// frontend/src/hooks/queries/auth-queries.ts
export function useMe(): UseQueryResult<PublicUser>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Profile hooks, profile page, and profile header components</name>
  <files>
    frontend/src/hooks/queries/profile-queries.ts,
    frontend/src/app/(app)/[username]/page.tsx,
    frontend/src/app/(app)/[username]/loading.tsx,
    frontend/src/components/profile/profile-header.tsx,
    frontend/src/components/profile/profile-stats.tsx,
    frontend/src/components/profile/profile-post-grid.tsx,
    frontend/src/components/profile/profile-skeleton.tsx
  </files>
  <action>
    1. Install missing shadcn components:
       ```
       cd frontend && npx shadcn@latest add avatar dialog textarea skeleton tabs separator scroll-area -y
       ```

    2. Create `frontend/src/hooks/queries/profile-queries.ts`:
       - `useProfile(username: string)`: TanStack Query, queryKey: ['profile', username], GET /profiles/{username}. staleTime: 5 * 60 * 1000.
       - `useUpdateProfile()`: useMutation, PATCH /profiles/me. onSuccess: invalidate ['profile'] queries and update auth store with new username if changed.
       - `useCheckUsername(username: string)`: useQuery with enabled: username.length >= 3. queryKey: ['username-check', username]. GET /profiles/check/{username}. staleTime: 30 * 1000. debounce not needed in query (TanStack handles refetch on key change).

    3. Create `frontend/src/components/profile/profile-stats.tsx`:
       - Three stat items in a row: posts count, followers count, following count
       - Followers and following are clickable (link to /{username}/followers and /{username}/following)
       - Vietnamese labels: "bai viet", "nguoi theo doi", "dang theo doi"
       - Format numbers (e.g., 1.2K for 1200) -- simple formatter function

    4. Create `frontend/src/components/profile/profile-header.tsx`:
       - Instagram-style layout per locked decisions:
         - Circular avatar on the left (use shadcn Avatar with AvatarFallback showing initials)
         - Stats row on the right (use ProfileStats component)
         - Display name, @username, and bio below
         - "Chinh sua trang ca nhan" (Edit Profile) button for own profile
         - Follow button placeholder for other users: render a Button with text "Theo doi" / "Dang theo doi" based on profile.isFollowing. Plan 02-04 will replace this with the real FollowButton that has optimistic updates. Use a data attribute `data-follow-placeholder` so Plan 02-04 can identify and replace it.
         - "Theo doi ban" (Follows you) badge when isFollowedBy is true, displayed near the follow button
       - Props: profile: ProfileResponse, onEditClick: () => void
       - Mobile responsive: stack avatar above stats on small screens, side-by-side on md+

    5. Create `frontend/src/components/profile/profile-post-grid.tsx`:
       - 3-column square thumbnail grid layout using CSS grid (grid-cols-3, aspect-square)
       - For now: empty state with "Chua co bai viet nao" message and camera icon (posts are Phase 3)
       - Component accepts posts array prop (empty for now) so Phase 3 can populate it

    6. Create `frontend/src/components/profile/profile-skeleton.tsx`:
       - Loading skeleton matching profile-header layout using shadcn Skeleton
       - Skeleton circles for avatar, rectangles for stats, text lines for name/bio

    7. Create `frontend/src/app/(app)/[username]/page.tsx`:
       - Client component ('use client')
       - Extract username from params (use `use()` from React for Next.js params Promise)
       - Call useProfile(username)
       - Show ProfileSkeleton while loading
       - Show "Nguoi dung khong ton tai" not-found state on error
       - Render ProfileHeader + Tabs component with "Bai viet" tab (containing ProfilePostGrid)
       - Tabs: use shadcn Tabs. Only "Bai viet" (Posts) tab for now. "Bo suu tap" (Collection) tab added in Phase 4.
       - State for edit modal open/close

    8. Create `frontend/src/app/(app)/[username]/loading.tsx`:
       - Export ProfileSkeleton as default for Next.js loading state
  </action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/Figly && pnpm turbo build</automated>
  </verify>
  <done>
    Profile page renders at /[username] with Instagram-style header (avatar left, stats right), display name, @username, bio, "Follows you" badge, edit/follow button placeholder, tabbed content with empty post grid. Loading skeleton displays during data fetch. Build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Edit profile modal, signup username field, and complete-profile interstitial</name>
  <files>
    frontend/src/components/profile/profile-edit-modal.tsx,
    frontend/src/app/(auth)/signup/page.tsx,
    frontend/src/components/auth/signup-form.tsx,
    frontend/src/app/(app)/complete-profile/page.tsx,
    frontend/src/components/profile/complete-profile-form.tsx,
    frontend/src/app/(app)/layout.tsx,
    frontend/src/stores/auth-store.ts
  </files>
  <action>
    1. Create `frontend/src/components/profile/profile-edit-modal.tsx`:
       - Use shadcn Dialog (not Drawer) per user decision (modal/drawer overlay, not separate page)
       - Props: open: boolean, onOpenChange: (open: boolean) => void, profile: ProfileResponse
       - Form fields using react-hook-form + Zod resolver:
         - Avatar: circular preview with "Doi anh dai dien" (Change avatar) button. On click, file input opens. On file select, upload via POST /media/upload, get back mediaId, set as avatarId. Show upload progress indicator.
         - Display name: Input, 1-50 chars
         - Username: Input with "@" prefix display, real-time availability check using useCheckUsername. Show green checkmark or red X. Debounce input by 300ms before triggering check. Show cooldown warning if applicable.
         - Bio: Textarea with character counter (X/150), Vietnamese placeholder "Gioi thieu ban than..."
       - Submit: calls useUpdateProfile mutation. On success: close modal, show toast "Cap nhat thanh cong" (sonner). On error: show toast with error message.
       - Vietnamese labels: "Anh dai dien", "Ten hien thi", "Ten nguoi dung", "Tieu su"

    2. Update `frontend/src/components/auth/signup-form.tsx`:
       - Add username field to the form: Input with "@" prefix, below name field
       - Add usernameSchema validation from @figly/shared
       - Add real-time availability check using useCheckUsername (same hook as edit modal)
       - Show availability indicator (green check / red X / loading spinner)
       - Vietnamese label: "Ten nguoi dung" with helper text "chi chua chu thuong, so, dau gach duoi va dau cham"
       - Update form submission to include username in signup request

    3. Update `frontend/src/app/(auth)/signup/page.tsx`:
       - No structural changes needed -- the SignupForm component handles the username field internally

    4. Create `frontend/src/components/profile/complete-profile-form.tsx`:
       - Simplified form with just username field (required) and optional display name update
       - Same real-time availability check as signup form
       - Submit calls PATCH /profiles/me with chosen username
       - On success: update auth store with new username, redirect to home

    5. Create `frontend/src/app/(app)/complete-profile/page.tsx`:
       - Client component showing CompleteProfileForm
       - Header: "Hoan thanh ho so" (Complete your profile)
       - Subtitle: "Chon ten nguoi dung de bat dau" (Choose a username to get started)
       - Centered card layout matching auth page style

    6. Update `frontend/src/app/(app)/layout.tsx`:
       - After existing auth check, add username check:
         - If user is authenticated and verified but user.username is null, redirect to /complete-profile
         - Exception: if current path IS /complete-profile, don't redirect (prevent loop)
       - This gates all (app) routes behind having a username

    7. Update `frontend/src/stores/auth-store.ts`:
       - Ensure PublicUser type includes username field (it was updated in 02-01 shared package)
       - No other changes needed if the store already uses PublicUser type
  </action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/Figly && pnpm turbo build</automated>
  </verify>
  <done>
    Edit profile modal opens from profile page with avatar upload, display name, username (with availability check and cooldown), and bio (with character counter). Signup form collects @username with real-time availability check. OAuth users without username are redirected to /complete-profile interstitial. All builds pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify profile page, edit modal, signup username, and complete-profile flow</name>
  <action>
    Human verification of the complete profile experience built in Tasks 1 and 2.

    Steps to verify:
    1. Start Docker and dev servers: `docker compose up -d && cd backend && npx prisma db push && cd .. && pnpm turbo dev`
    2. Sign up with a new account at http://localhost:3000/signup -- verify username field is present with real-time availability check
    3. After signup and email verification, visit http://localhost:3000/{your-username} -- verify profile page shows:
       - Circular avatar placeholder on the left
       - Stats (0 posts, 0 followers, 0 following) on the right
       - Display name and @username below
       - "Chinh sua trang ca nhan" button
       - Empty post grid with "Chua co bai viet nao"
    4. Click "Chinh sua trang ca nhan" -- verify edit modal opens with:
       - Avatar upload area
       - Display name, username (with @), bio (with counter) fields
       - Save changes and verify toast confirmation
    5. Try changing username -- verify availability check shows green/red indicator
    6. Verify mobile responsive layout (resize browser or use DevTools)
  </action>
  <verify>Human confirms visual layout and interactive flows work correctly</verify>
  <done>Profile page layout matches Instagram-style design. Edit modal saves changes. Signup collects username. Complete-profile gate works for OAuth users.</done>
</task>

</tasks>

<verification>
1. `pnpm turbo build` -- all workspaces build successfully
2. Profile page renders at /[username] with correct layout
3. Edit profile modal opens and saves changes
4. Signup form includes username field
5. Complete-profile interstitial gates users without username
</verification>

<success_criteria>
- Profile page at /[username] displays Instagram-style header with avatar, stats, name, username, bio
- Edit profile modal allows changing avatar, display name, username (with cooldown), and bio
- Username availability check works in real-time on both signup and edit forms
- Post grid shows empty state placeholder
- "Follows you" badge displays when applicable
- OAuth users without username are redirected to complete-profile page
- All builds pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-profiles-social-graph/02-03-SUMMARY.md`
</output>
