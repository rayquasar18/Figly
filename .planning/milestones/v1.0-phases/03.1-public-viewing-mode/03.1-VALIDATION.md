---
phase: 3.1
slug: public-viewing-mode
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-14
---

# Phase 3.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Jest 29 + ts-jest |
| **Config file** | `backend/jest.config.ts` |
| **Quick run command** | `cd backend && npx jest --testPathPattern="public" -x` |
| **Full suite command** | `cd backend && npx jest` |
| **Estimated runtime** | ~30 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cd backend && npx jest --testPathPattern="public" -x`
- **After every plan wave:** Run `cd backend && npx jest`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 03.1-01-01 | 01 | 1 | PUB-06 | unit | `cd backend && npx jest --testPathPattern="optional-jwt" -x` | ❌ W0 | ⬜ pending |
| 03.1-01-02 | 01 | 1 | PUB-01,PUB-02,PUB-03 | integration | `cd backend && npx jest --testPathPattern="public-access" -x` | ❌ W0 | ⬜ pending |
| 03.1-01-03 | 01 | 1 | PUB-04 | integration | `cd backend && npx jest --testPathPattern="public-feed" -x` | ❌ W0 | ⬜ pending |
| 03.1-01-04 | 01 | 1 | PUB-05 | integration | `cd backend && npx jest --testPathPattern="public-access" -x` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `backend/src/auth/__tests__/optional-jwt-auth.guard.spec.ts` — unit tests for OptionalJwtAuthGuard (PUB-06)
- [ ] `backend/src/posts/__tests__/public-access.spec.ts` — integration tests for public read endpoints (PUB-01 through PUB-05)
- [ ] `backend/src/feed/__tests__/public-feed.spec.ts` — integration tests for public feed endpoint (PUB-04)

*Existing test infrastructure covers framework setup — only phase-specific test files needed.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Public pages render without login redirect | PUB-06 | Requires browser navigation behavior | Open incognito browser, navigate to /:username — page should render profile without redirect |
| Interactive buttons redirect to login on click | PUB-05 | Client-side interaction flow | On public profile page, click like button — should redirect to /login |
| Bottom nav shows login CTA for unauthenticated | PUB-05 | Visual UI verification | Open incognito browser, verify nav shows Login/Signup instead of full nav |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
