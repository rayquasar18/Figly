---
phase: 04-collection-system
plan: 06
type: execute
wave: 5
depends_on: ["04-03", "04-04", "04-05"]
files_modified:
  - frontend/src/components/collection/item-picker.tsx
  - frontend/src/components/create-post/step-caption.tsx
  - frontend/src/components/post/post-card.tsx
  - frontend/src/components/post/post-detail-modal.tsx
  - frontend/src/stores/create-post-store.ts
  - frontend/src/app/(app)/checklists/[checklistId]/page.tsx
autonomous: true
requirements:
  - CONT-07

must_haves:
  truths:
    - "When creating/editing a post, user can link items from the collection database via a search picker"
    - "Post detail shows linked items as tappable tags below the caption"
    - "ItemPicker supports single-select mode (for checklists) and multi-select mode (for posts)"
  artifacts:
    - path: "frontend/src/components/collection/item-picker.tsx"
      provides: "Search+select picker dialog for linking items to posts and checklists"
      min_lines: 50
    - path: "frontend/src/components/create-post/step-caption.tsx"
      provides: "Extended caption step with item linking button and selected item chips"
      contains: "ItemPicker"
    - path: "frontend/src/components/post/post-card.tsx"
      provides: "Extended post card showing linked item badges"
      contains: "linkedItems"
  key_links:
    - from: "frontend/src/components/collection/item-picker.tsx"
      to: "collection-queries.ts"
      via: "useSearchItems hook for item selection"
      pattern: "useSearchItems"
    - from: "frontend/src/components/create-post/step-caption.tsx"
      to: "item-picker.tsx"
      via: "ItemPicker dialog opened from caption step"
      pattern: "ItemPicker"
    - from: "frontend/src/stores/create-post-store.ts"
      to: "frontend/src/components/create-post/step-caption.tsx"
      via: "linkedItemIds stored in create-post store, sent with post creation"
      pattern: "linkedItemIds"
---

<objective>
Build the ItemPicker dialog component and integrate post-to-item linking into the create post flow and post display. Also wire ItemPicker into the checklist detail page for adding database items.

Purpose: Allow users to link posts to collection items (CONT-07) and improve checklist item-adding UX with a proper search picker.
Output: ItemPicker component, extended create-post flow, extended post display, checklist integration
</objective>

<execution_context>
@/Users/mqcbook/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mqcbook/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-collection-system/04-CONTEXT.md
@.planning/phases/04-collection-system/04-RESEARCH.md
@.planning/phases/04-collection-system/04-01-SUMMARY.md
@.planning/phases/04-collection-system/04-03-SUMMARY.md
@.planning/phases/04-collection-system/04-04-SUMMARY.md

@frontend/src/components/create-post/step-caption.tsx
@frontend/src/components/create-post/create-post-flow.tsx
@frontend/src/stores/create-post-store.ts
@frontend/src/components/post/post-card.tsx
@frontend/src/components/post/post-detail-modal.tsx
@frontend/src/hooks/queries/collection-queries.ts
@packages/shared/src/types/collection.types.ts
@packages/shared/src/types/post.types.ts
@packages/shared/src/dto/post.dto.ts

<interfaces>
<!-- PostResponse extended in Plan 03 with linkedItems -->
From packages/shared/src/types/post.types.ts (extended in Plan 03):
```typescript
export interface PostResponse {
  // ...existing fields...
  linkedItems?: LinkedItemResponse[];
}
```

From packages/shared/src/types/collection.types.ts:
```typescript
export interface LinkedItemResponse {
  id: string; name: string; seriesName: string;
  categoryName: string; imageUrl: string | null;
}
```

<!-- Existing create-post store pattern -->
From frontend/src/stores/create-post-store.ts:
```typescript
// Zustand store with: images, caption, step, setCaption, etc.
// Need to add: linkedItemIds: string[], addLinkedItem, removeLinkedItem
```

<!-- Collection queries from Plan 04 -->
From frontend/src/hooks/queries/collection-queries.ts (created in Plan 04):
```typescript
export function useSearchItems(query, filters): UseInfiniteQueryResult
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ItemPicker dialog and create-post store extension</name>
  <files>
    frontend/src/components/collection/item-picker.tsx,
    frontend/src/stores/create-post-store.ts
  </files>
  <action>
    **ItemPicker (frontend/src/components/collection/item-picker.tsx):**
    Dialog-based item picker for selecting collection items:
    - shadcn Dialog wrapping the picker UI
    - Search input with debounced search (300ms) using useSearchItems from collection-queries
    - Search results as a scrollable list of item rows (name, series, category, small image)
    - Click to select/deselect -- selected items shown as Badge chips below search
    - "Xong" (Done) button closes dialog and passes selected item IDs back via callback
    - Prop interface: `{ mode: 'single' | 'multi', selectedItemIds: string[], onSelect: (ids: string[]) => void, open: boolean, onOpenChange: (open: boolean) => void }`
    - `single` mode: selecting an item immediately calls onSelect with [itemId] and closes the dialog
    - `multi` mode: selecting toggles items, done button confirms full selection

    **Extend create-post-store (frontend/src/stores/create-post-store.ts):**
    Add to the store:
    - `linkedItemIds: string[]` (default: [])
    - `linkedItems: LinkedItemResponse[]` (default: [] -- for display)
    - `addLinkedItem(item: LinkedItemResponse)`: adds to both arrays
    - `removeLinkedItem(itemId: string)`: removes from both arrays
    - `reset()`: also clears linkedItemIds and linkedItems
  </action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/Figly && npx tsc --noEmit -p frontend/tsconfig.json 2>&1 | head -30 || echo "TS check done"</automated>
  </verify>
  <done>ItemPicker component supports single and multi mode with search. Create-post store has linkedItemIds and linkedItems state with add/remove/reset.</done>
</task>

<task type="auto">
  <name>Task 2: Wire item linking into post creation flow and post display</name>
  <files>
    frontend/src/components/create-post/step-caption.tsx,
    frontend/src/components/post/post-card.tsx,
    frontend/src/components/post/post-detail-modal.tsx,
    frontend/src/app/(app)/checklists/[checklistId]/page.tsx
  </files>
  <action>
    **Extend StepCaption (frontend/src/components/create-post/step-caption.tsx):**
    Add below the caption textarea area:
    - "Lien ket vat pham" (Link items) button with Link icon that opens ItemPicker dialog in multi mode
    - Below button: show selected items as removable Badge chips (item name + X to remove)
    - Clicking X calls removeLinkedItem from store
    - ItemPicker onSelect updates the store

    **Extend CreatePostFlow / post creation API call:**
    When submitting the post, include `linkedItemIds` from the store in the createPost API call body. The shared createPostSchema already has linkedItemIds (added in Plan 03 Task 2).

    **Extend PostCard (frontend/src/components/post/post-card.tsx):**
    If post has `linkedItems` and it's non-empty, render below the caption:
    - Row of tappable Badge components, each showing item name
    - Clicking a badge navigates to `/item/${itemId}`
    - Use Package icon prefix in each badge
    - Max 3 visible, "+N more" if more than 3

    **Extend PostDetailModal (frontend/src/components/post/post-detail-modal.tsx):**
    Same as PostCard but show all linked items (no limit), in a small section between caption and comments.

    **Wire ItemPicker in Checklist Detail (frontend/src/app/(app)/checklists/[checklistId]/page.tsx):**
    The checklist detail page (created in Plan 05 Task 1) has a "Them tu database" button. Wire it to open the ItemPicker dialog in single-select mode. When user selects an item from the picker, call useAddEntry with the selected itemId.
  </action>
  <verify>
    <automated>cd /Users/mqcbook/Documents/github/my-source/Figly && npx tsc --noEmit -p frontend/tsconfig.json 2>&1 | head -30 || echo "TS check done"</automated>
  </verify>
  <done>StepCaption shows link items button and selected item chips. Posts created with linked items. PostCard shows linked item badges (max 3 + overflow). PostDetailModal shows all linked items. Checklist detail uses ItemPicker in single-select mode for adding database items.</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `cd frontend && npx tsc --noEmit`
2. Post creation includes item linking step with ItemPicker
3. Linked items appear as badges on PostCard and PostDetailModal
4. Checklist detail "Them tu database" opens ItemPicker in single mode
5. Full backend test suite still passes: `cd backend && npx jest --no-coverage`
</verification>

<success_criteria>
- ItemPicker: search and select items in both single and multi mode
- Post linking: item picker in caption step, selected items as chips, linked items as badges on post card/detail
- Checklist integration: ItemPicker wired for adding database items
- Vietnamese text throughout
</success_criteria>

<output>
After completion, create `.planning/phases/04-collection-system/04-06-SUMMARY.md`
</output>
